window.__BOARD = {"project": "Stock Portfolio Builder", "updated": "2026-08-24", "tagline": "Project board \u2014 features flow left\u2192right through the pipeline. Edit this file (docs/board.json) and run tools/render_dashboard.py to update both DASHBOARD.md and dashboard.html. Each card's title is the feature; the bullets describe, in plain language, what it does and the logic behind it.", "stages": [{"key": "ideation", "label": "Ideation", "desc": "A half-baked idea; can be pushed further down once fleshed out."}, {"key": "design", "label": "Design", "desc": "Detailed requirements captured; a spec exists in docs/features/."}, {"key": "implementation", "label": "Implementation", "desc": "Being built on the dev branch."}, {"key": "testing", "label": "Testing", "desc": "Built; waiting for you to try it."}, {"key": "refinement", "label": "Refinement", "desc": "Tested but not yet approved; new instructions \u2192 back to Implementation."}, {"key": "integration", "label": "Integration", "desc": "Approved; merging dev \u2192 main (prod) + updating docs."}, {"key": "done", "label": "Done", "desc": "Integrated into the product (on main)."}], "epics": [{"key": "core", "name": "Core tool (shipped)", "color": "#13605C"}, {"key": "E1", "name": "E1 \u00b7 User-Defined Themes", "color": "#3A6EA5"}, {"key": "E2", "name": "E2 \u00b7 User-Defined Metrics", "color": "#A4683C"}, {"key": "E3", "name": "E3 \u00b7 Statement-Driven Data", "color": "#6B5B8C"}, {"key": "E4", "name": "E4 \u00b7 Fundamentals & Screener workflow", "color": "#4D7C6F"}, {"key": "E7", "name": "E7 \u00b7 First-run onboarding", "color": "#8C5B6B"}, {"key": "E10", "name": "E10 \u00b7 History retention", "color": "#4F8CB0"}, {"key": "APP", "name": "APP \u00b7 iOS app", "color": "#B0403A"}, {"key": "E5", "name": "E5 \u00b7 Web UI overhaul", "color": "#0E7C6B"}, {"key": "E6", "name": "E6 \u00b7 Multi-user platform", "color": "#5B6B8C"}, {"key": "E11", "name": "E11 \u00b7 Online, for invited users", "color": "#7A6BA8"}], "items": [{"id": "E11.0", "epic": "E11", "stage": "done", "platform": "web", "depends": "E6.5", "spec": "docs/features/E11_online-deployment.md", "title": "Spike: does Yahoo work from a Cloudflare IP", "notes": ["DONE 2026-08-16. Throwaway Worker doing only the cookie+crumb handshake plus one quote call, deployed to workers.dev and hit repeatedly.", "RESULT: PASS. 0 failures in 10 runs, no rate limiting, 25-symbol batch returned 200 in 70ms, avg 310ms for five Yahoo calls with a FRESH crumb every time (worst case - real use caches it).", "Also settled two open questions: /v8/finance/chart needs no cookie and no crumb; and the 10ms Workers CPU limit is not threatened by fetching, since all of that time is I/O which does not count against CPU.", "WHY IT MATTERED: Yahoo deployed TLS fingerprinting in Apr 2025 and a Worker cannot control its TLS fingerprint. This is the only evidence that exists either way for Cloudflare specifically - a web search found none. Delete the spike Worker at cutover."]}, {"id": "E11.1", "epic": "E11", "stage": "done", "platform": "web", "depends": "E6.5", "spec": "docs/features/E11_online-deployment.md", "title": "Nightly pg_dump, and a restore actually performed", "notes": ["DONE 2026-08-16, and PROVEN: the run reported portfolio rows restored: 1 / restore verified: 1 row(s), all carrying a portfolio document. The dump is recoverable, not merely present.", "Supabase Free provides ZERO backups in 2026 - no daily snapshots, no PITR, no retention. Their own docs tell free users to dump it themselves.", "With one JSON row per user, no device copy (E9) and no export, a single bad write is unrecoverable total loss for that user. Even Pro only restores the WHOLE project to a point in time; there is no row-level restore at any price.", "MUST use the session pooler on port 5432, not 6543 and not the direct endpoint: the direct endpoint is IPv6-only and GitHub runners are IPv4-only, so a direct dump fails with 'Network is unreachable'.", "The deliverable is the RESTORE, not the dump. An untested backup is a guess. Restore into a local supabase instance once and confirm the book opens.", "TRAP: GitHub disables scheduled workflows after 60 days without repo commits - the backup and any keep-alive die together, silently, exactly when the project is quiet enough to need them."]}, {"id": "E11.2a", "epic": "E11", "stage": "done", "platform": "web", "depends": "E11.0", "spec": "docs/features/E11_online-deployment.md", "title": "Worker: transport + quotes, search, peers", "notes": ["DONE 2026-08-16. pb-proxy deployed. DIFFED against server.py: quotes IDENTICAL across 8 symbols and 112 fields, peers identical. The harness (tools/diff_proxy.py) is reusable and E11.2b needs it.", "SEARCH RANKING BUG FOUND BY THE HARNESS, in server.py not the Worker: name matches all shared one rank and then tie-broke on SYMBOL LENGTH, so 'Maui Land & Pineapple' (MLP, 3 chars) outranked 'Apple Inc.' (AAPL, 4) for the query apple. Now ranked by WHERE the name matches - starts-with, then whole word, then anywhere.", "OPEN DESIGN QUESTION, deliberately not decided here: server.py searches the cached SEC company directory, which lists operating companies and NOT ETFs, so it can never return VOO or SPY; the Worker uses Yahoo search, which can. After E11.3 the Worker IS production, so local dev would rank differently from prod. The harness asserts SHAPE (the client contract) and reports ranking as different-by-design rather than failing.", "Split out of E11.2 so it fits a single session start-to-merge. A half-built data layer is exactly what must never be left outstanding.", "The transport is the real work: swap CapacitorHttp for fetch, add the browser User-Agent (Yahoo blocks generic agents) and MANUAL cookie handling, because Workers have no cookie jar.", "Cache the crumb across requests. The spike refetched it every time, which is two wasted upstream calls per request.", "Diff every field against server.py for the same symbols before it counts as done. A data-layer rewrite must not go in on inspection alone."]}, {"id": "E11.2b", "epic": "E11", "stage": "done", "platform": "web", "depends": "E11.2a", "spec": "docs/features/E11_online-deployment.md", "title": "Worker: fundamentals + statements", "notes": ["DONE 2026-08-16. Worker now serves all five endpoints. DIFFED against server.py: 1027 fields IDENTICAL across quotes, fundamentals, statements and peers.", "The claimed nativeFundamentals field gap DOES NOT EXIST - the JS client already mirrors server.py exactly (my extraction missed ES6 shorthand). The APP_MIGRATION note saying otherwise is stale.", "HARNESS FOUND 41 DIFFERENCES ON THE FIRST RUN, all mine. (1) rv() returned Yahoo's bare {} instead of null, and undefined keys are DROPPED by JSON.stringify - so a sparse symbol came back missing a dozen fields the client expects to exist, while Python emitted them as null.", "(2) SERVER.PY DEGRADED EVERY ETF TO A PLACEHOLDER. get_fundamentals decided whether a live response was usable by testing peg-or-ev - STOCK metrics an ETF can never have - so SPY, VOO and QQQ had their real name, price, P/E and dividend yield discarded in favour of a seed row that is empty for any symbol outside SEED. Now tests whether a real answer came back at all. Directly serves the owner requirement that ETFs must work.", "The field-heavy pair. Includes closing the known nativeFundamentals gap so the Worker returns the same ~21 E2 fields server.py does - otherwise the deployed app silently serves a thinner model than local dev.", "Both fan out one request per symbol; the shared yMapLimit caps that at 4.", "DO NOT DEPLOY /api/portfolio - unauthenticated read/write of a server-side file. It is only reachable behind isPrivateHost() and must not exist in the deployed proxy."]}, {"id": "E11.9", "epic": "E11", "stage": "done", "platform": "web", "depends": "E11.2b", "spec": "docs/features/E11_online-deployment.md", "title": "ETFs are researchable, never model members", "notes": ["OWNER 2026-08-16: users should be able to look up ETFs in Research with no problem, but must NOT be able to add them to a portfolio, because that breaks the premise - the model allocates capital by favouring company characteristics (PEG, FCF, EV/EBITDA).", "WHY IT IS WORSE THAN SCORING BADLY: metrics with useMedian fill a missing value with the THEME MEDIAN, so an ETF left in a theme would receive real capital on entirely invented inputs. Silently plausible, which is the dangerous kind.", "quoteType now comes back from ALL THREE proxies - server.py, the Worker and the native JS client - because a rule enforced from data only one of them returns is a rule that drifts. isEquity() is the single definition; unknown types are allowed and settled by the fetch.", "The guard sits at the TOP of setMembership, the one choke point every membership change routes through (addTicker, removeTicker, doSwap), and BEFORE state.themeTickers is written five lines below. A newly typed symbol is fetched first so the decision is made on a real answer. Verified: an ETF is refused, membership is byte-identical afterwards, and nothing is saved.", "Research is deliberately untouched: search, view and watchlist anything. Search rows now label the type, so an ETF reads as one before anyone tries to add it, and openSwap refuses without opening a modal."]}, {"id": "E11.10", "epic": "E11", "stage": "done", "platform": "web", "depends": "E11.9", "spec": "docs/features/E11_online-deployment.md", "title": "Ticker search runs locally, and covers ETFs", "notes": ["OWNER 2026-08-16: the search box must cover ETFs, and every live Yahoo lookup is a request that is slow and risks a block. So cache a broad ticker universe and refresh it weekly.", "tools/build_ticker_directory.py builds www/data/tickers.json from the NASDAQ Trader symbol directory - no key, no bot wall on that host. 11,290 symbols: 5,575 ETFs and 5,715 equities across Nasdaq, NYSE, NYSE American, NYSE Arca, Cboe BZX and IEX, each with a real is-ETF flag. Weekly refresh workflow, which also keeps the backup cron alive (GitHub disables cron after 60 days of repo inactivity).", "THREE THINGS THAT WOULD HAVE BROKEN IT QUIETLY: Nasdaq's published field definitions are STALE and omit the ETF column the live file has, so everything parses by header NAME; Nasdaq writes BRK.A where Yahoo writes BRK-A, so symbols are normalised at the source or the directory would offer tickers that fail to fetch; and a truncated download must never replace a good index, so the generator validates counts and known symbols and swaps atomically.", "Search now matches in-process: 12 queries in 24ms with ZERO network calls, against one 689KB load. Yahoo remains the fallback for OTC names and anything listed since the last refresh.", "I REINTRODUCED THE server.py RANKING BUG AND THE TESTS CAUGHT IT: tie-breaking name matches by symbol length made 'appl' return AIT (Applied Industrial) before AAPL. Name matches now tie-break on how much of the name's FIRST WORD the query covers - 4 of 5 letters of Apple beats 4 of 7 of Applied."]}, {"id": "E11.3a", "epic": "E11", "stage": "done", "platform": "web", "depends": "E11.10", "spec": "docs/features/E11_online-deployment.md", "title": "Pre-deploy hygiene: noindex, robots, build stamp", "notes": ["DONE 2026-08-16. Split out of E11.3 so the deploy ticket is only the deploy. All three are testable locally, so there was no reason for them to ride along with a first Pages deploy and be verified under pressure.", "robots.txt disallows crawling, and a noindex meta covers a crawler that reads the page anyway. The URL is meant to be reached by people who were sent it - an indexed sign-in page is exactly how a stranger arrives at an invite-only tool.", "APP_VERSION is stamped from the git commit by tools/stamp_version.py and shown in the Account panel. Which build are you on is the first question any bug report needs, and it was unanswerable. The comment referencing the stamper was written before the stamper existed; the tool was created rather than the comment softened."]}, {"id": "E11.11", "epic": "E11", "stage": "done", "platform": "web", "depends": "E7.2", "spec": "docs/features/E11_online-deployment.md", "title": "Fix: the guided-tour bubble was transparent", "notes": ["REPORTED BY THE OWNER 2026-08-16 from a screenshot: the tour popup was see-through, so the page behind it read straight through the text and the step was unreadable.", "ROOT CAUSE: .coach used background:var(--surface), and --surface IS NOT DEFINED ANYWHERE. Every other rule in the file uses --surface2. An undefined custom property makes the whole declaration invalid at computed-value time, so background fell back to its initial value - transparent. It affected both themes and the arrow too.", "Fixed to var(--paper): #FFFFFF light, #121A18 dark, verified opaque in both. The bubble stays above the ring at z-index 301, so it covers the page while a step is up but never the element being highlighted.", "MADE IT UNREPEATABLE: check_syntax.py now fails when a CSS variable is used without a fallback and never defined. This class of bug is silent - nothing logs, nothing throws, it just renders wrong - so a gate is the only thing that would have caught it. Scanned the whole file: --surface was the only offender. Verified the check exits 1 on the reintroduced bug and 0 on the fixed file."]}, {"id": "E11.12", "epic": "E11", "stage": "done", "platform": "web", "depends": "E3.5", "spec": "docs/features/E11_online-deployment.md", "title": "Fix: money scaled by sign instead of magnitude", "notes": ["REPORTED BY THE OWNER 2026-08-16 from a screenshot: the Cash flow table mixed units - operating cash flow read $30.0B while capital expenditure on the row below read $-18829M.", "ROOT CAUSE: bil() tested the SIGNED value (v>=1e9), so a negative could never reach the billions branch and fell straight through to millions. Every negative line - capex, dividends paid, buybacks, tax provision - was printed one scale too small. There was also no K branch at all, so anything under a million collapsed to $0M.", "volFmt(), on the very next line of the file, had always used Math.abs() correctly. The two were never compared. Same lesson as the search tie-break: the right version of a thing often already exists nearby.", "Now scales on Math.abs: >=1T T, >=1B B, >=1M M, otherwise K, with zero printed plainly as $0. Verified against the exact values in the screenshot plus both boundaries in each direction.", "Also fixed share counts, which were hardcoded to billions - a company with 50 million shares read 0.05B. Routed through volFmt, which already scaled correctly."]}, {"id": "E11.13", "epic": "E11", "stage": "done", "platform": "web", "depends": "E6.4", "spec": "docs/features/E11_online-deployment.md", "title": "Idle session timeout - sign out after inactivity", "notes": ["DONE 2026-08-18 at FIVE minutes (owner's decision after I flagged that 2 was very short). Warns for the last 30 seconds with a live countdown and a Stay signed in button; any pointer, key, touch or wheel event cancels it.", "THE TRAP THAT WOULD HAVE MADE IT USELESS: a hidden tab still counts down, so on returning after ten minutes the pointermove from reaching for the window fires BEFORE the next tick and would have reset the clock - the feature silently never firing for the exact case it exists for. idleBump() refuses to reset once the deadline has passed, and visibilitychange re-checks immediately rather than waiting up to a second on a throttled background timer.", "'scroll' is deliberately NOT an activity event: renderRebalanceOutput calls scrollIntoView, so the APP would have kept its own session alive. Same shape as counting the 60-second price poll.", "Started and stopped exactly where the price timer is - both sign-in paths and lockOut - so the idle clock cannot outlive the account it belongs to.", "Verified 18 ways including both traps, and that the warning is opaque and sits above the coach bubble but below the gate.", "OWNER 2026-08-16: sign people out once they have been inactive for 2 minutes or more, for safety. NOT STARTED - captured at the end of the session.", "WORTH CONFIRMING BEFORE BUILDING: 2 minutes is very short for an idle timeout. Reading the statements table, or thinking about a rebalance plan, routinely takes longer than that, so the likely experience is being signed out mid-thought several times an hour. 15 minutes is the usual floor for a finance tool. The owner said 2 minutes OR MORE, so the requirement is a minimum - worth agreeing the actual number first, and possibly making it a setting.", "ACTIVITY MUST MEAN THE USER, NOT THE APP. The auto-refresh timer fires every 60s and must NOT count as activity, or the session never expires while a tab is open - which is exactly the case this exists to protect against. Count pointer, key, touch and visibility events only. Same class of mistake as a guard that does not measure the thing it guards against.", "Warn before acting. A silent logout looks like a crash; a short countdown that any input cancels does not.", "WHAT IS LOST: nothing already saved. Membership and rebalances persist immediately, and E9 means nothing about the portfolio is on the device anyway. The exception is an UNAPPLIED rebalance plan held in lastPlan - decide whether to warn about that specifically, or accept it.", "The teardown already exists and is proven: lockOut() clears account state, stops timers and raises the gate. This ticket is the trigger, not the mechanism.", "Consider whether it should apply while the tab is hidden (probably yes - an unattended screen is the risk) and whether the countdown should survive a reload."]}, {"id": "E11.3", "epic": "E11", "stage": "done", "platform": "web", "depends": "E11.2", "spec": "docs/features/E11_online-deployment.md", "title": "Deploy to Cloudflare Pages, same-origin /api/*", "notes": ["DONE 2026-08-18. LIVE at https://portfolio-builder-esb.pages.dev - app and proxy on one origin, so the client's relative api() calls work untouched.", "A *.pages.dev domain cannot carry a Worker route (routes need a zone), so Pages Functions is the only way onto the same origin without buying a domain. wrangler now steers new projects to Workers-with-assets; Pages was kept for the shorter, more demo-able URL.", "The Function is a FOUR-LINE ADAPTER that imports worker/src/index.js rather than a copy. There were already three implementations of the Yahoo client and two drifted apart twice - a fourth copy would be a fourth thing to forget.", "PROVEN not just reachable: diff_proxy.py against the DEPLOYED origin = 1,035 fields identical to server.py. Then 16 checks in a real phone-sized browser on the live origin: gate up, manifest parses, ticker directory loads from that origin, 'appl' returns AAPL with zero /api calls, and same-origin /api/quotes returns live data.", "GOTCHA WORTH REMEMBERING: Pages serves index.html with HTTP 200 for ANY unknown path. A 200 therefore does not prove a file exists - /proto/newui.html and /definitely-not-a-page both return the app byte-for-byte. Verification has to compare CONTENT, not status. Confirmed every data asset is the real file and not fallback HTML. The Function's own 404 for an unknown /api route is not shadowed by this.", "Icons generated from code (tools/build_icons.py, stdlib zlib+struct) rather than committed as binaries, and verified by decoding the PNG back and checking the mark draws. Moved www/proto/ to docs/proto/ so a stale prototype is not publicly served.", "pb-yahoo-spike DELETED at cutover as planned. pb-proxy is now REDUNDANT - Pages serves the same code - and is a second unauthenticated public Yahoo proxy; left running deliberately as an independent test target while E11.4/E11.7 are in flight, but it should go.", "STILL NEEDS THE OWNER: Supabase Auth -> Site URL + Redirect URLs as https://portfolio-builder-esb.pages.dev/** (DOUBLE asterisk). Sign-in with an existing account already works without it; what breaks without it is email CONFIRMATION and PASSWORD RESET links, which still point at localhost.", "api() uses RELATIVE paths, so /api/* must be same-origin with the page - which is why Pages plus a Worker route, rather than two separate services.", "Add robots.txt and a noindex meta. The URL is meant to be unlisted; an indexed sign-in page is how strangers find it.", "Add a web app manifest and icon so it opens from the phone home screen without a URL bar - that IS the demo requirement.", "Show the app version in the UI, or the first bug report is unanswerable.", "server.py stays as the local dev server, unchanged. Two environments, one codebase.", "Supabase Auth: Site URL plus Redirect URLs as https://<host>/** - note the DOUBLE asterisk, /* does not match nested paths. Keep the localhost entries so local dev keeps working."]}, {"id": "E11.4", "epic": "E11", "stage": "design", "platform": "web", "depends": "E11.3", "spec": "docs/features/E11_online-deployment.md", "title": "Custom SMTP, so anyone but the owner can sign up", "notes": ["OWNER-BLOCKING and not a free-tier issue: Supabase's built-in mailer 'will refuse to deliver messages to addresses that are not part of the project's team', per their docs. Paying for Pro does NOT fix it. Custom SMTP does, and is available on the Free plan.", "Gmail app password (no domain, ~500/day) or Resend plus SPF/DKIM/DMARC (needs a domain). Gmail first; the domain is the upgrade.", "Gmail/Yahoo moved to PERMANENT 550 rejections for unauthenticated senders in Nov 2025, so a domain sender without SPF/DKIM/DMARC is worse than no domain at all.", "Turnstile on signup - free, natively supported by Supabase. An unlisted URL still gets found by bots, and scripted signups burn the email quota and wreck sender reputation via bounces."]}, {"id": "E11.5", "epic": "E11", "stage": "done", "platform": "web", "depends": "E11.3", "spec": "docs/features/E11_online-deployment.md", "title": "Export and delete: the user's own data, in their hands", "notes": ["DONE 2026-08-16. Export serialises the document that WOULD BE SAVED (syncStateIntoDocument), so it cannot drift from the thing it copies; verified to leak no session tokens. Deletion via a SECURITY DEFINER function - sql/002_delete_own_account.sql, INSTALLED and confirmed prosecdef=true with execute granted to authenticated. A failed delete reports the cause and leaves the session intact rather than claiming success.", "Export is the user-facing half of the backup story and the thing that makes 'no device copy' honest. JSON of holdings, checkpoints, themes, watchlist and settings.", "Self-serve account deletion with a confirmation. The database cascade is already proven (isolation gate 8/8) - this is the UI for it.", "Both are data-protection requirements the moment someone else's data is stored, and both are small."]}, {"id": "E11.6", "epic": "E11", "stage": "done", "platform": "web", "depends": "E11.3", "spec": "docs/features/E11_online-deployment.md", "title": "Disclaimer and privacy note", "notes": ["DONE 2026-08-16. Disclaimer plus what-we-store, reachable FROM THE GATE - which needed a new .over-gate class at z-index 500, because the gate is 400 and .modal-backdrop is 80, so it would have rendered invisibly behind it. Verified hit-testable at its own centre, not merely present in the DOM.", "The app currently contains NO legal text at all - zero matches for terms, privacy, disclaimer or 'not investment advice'.", "Free and non-commercial is what keeps this outside 'investment adviser': the Advisers Act test is Advice + Business + COMPENSATION + Securities, and only compensation is missing. The publisher's exemption would NOT rescue it - Lowe v. SEC requires IMPERSONAL advice, and this is personalised by design.", "So: informational only, not investment advice, no adviser relationship, data as-is with no warranty. Shown at signup and reachable from the app.", "Plain privacy note: what is stored, where, who can see it, how to delete it. A posted policy has no size threshold under CalOPPA. NOT a full ToS/LLC/insurance stack - that is for commercial."]}, {"id": "E11.7a", "epic": "E11", "stage": "done", "platform": "web", "depends": "E11.3", "spec": "docs/features/E11_online-deployment.md", "title": "Feedback channel", "notes": ["DONE 2026-08-18. Split from E11.7 because the Sentry half needs an account the owner has and I do not; shipping the half that works beats blocking both.", "WRITE-ONLY BY DESIGN. sql/003_feedback.sql grants INSERT and nothing else - no select policy at all - so a user cannot read anyone's feedback back, including their own. That is also why the client sends Prefer: return=minimal: asking PostgREST to return the inserted row would fail on a table behaving exactly as intended. The owner reads it in the dashboard, which is not subject to RLS.", "Carries app_version, which is the entire reason E11.3a added the build stamp - a report you cannot pin to a build is hard to act on. Nothing else about the user is attached beyond their id.", "ON DELETE CASCADE on user_id, so deleting an account takes its feedback with it and E11.5 stays complete.", "Fails honestly: until the SQL is applied the insert 404s, and the modal says the table has not been created yet, keeps the button live, and PRESERVES the typed text rather than losing what the user wrote. Verified 15 ways.", "NEEDS THE OWNER: run sql/003_feedback.sql in the Supabase SQL editor."]}, {"id": "E11.7b", "epic": "E11", "stage": "design", "platform": "web", "depends": "E11.3", "spec": "docs/features/E11_online-deployment.md", "title": "Error monitoring (Sentry)", "notes": ["NOT STARTED - blocked on an account only the owner can create. Sentry's free Developer tier is 5k errors/month and needs no card.", "WHY IT MATTERS: there are ~19 console.error calls that vanish into browsers nobody can see. Now that the app is deployed and reachable from a phone, a failure the owner hits away from the laptop leaves no trace at all.", "When the DSN exists this is small: init early, and let it capture what already gets logged. Do not add a second error path."]}, {"id": "E11.8", "epic": "E11", "stage": "done", "platform": "web", "depends": "E11.3", "spec": "docs/features/E11_online-deployment.md", "title": "Tests and CI", "notes": ["DONE 2026-08-16. 12 Playwright tests against the real page, signed out, so CI carries no secrets. MUTATION-TESTED: reintroducing the E10.1 equal-weight defect fails exactly one test (received 100, the cash, against the expected 800, the real book value) and the other 11 still pass - the suite would have caught the bug that shipped.", "There are no tests and no CI today - tools/ holds check_syntax.py and two generators.", "Playwright against the real page, running the same in-page assertions used by hand throughout E10. No login needed, so CI carries no secrets.", "Target the MATH, not coverage: computeAllocation, planTrades, applyRebalance, computeCarryover, applyThemeCap, the version timeline, and the equal-weight reconstruction across a trim. The dangerous bug in a portfolio tool is a plausible wrong number, not a crash - a crash gets reported.", "Justification, from this project's own history: E10.1 shipped a defect that made the Overview state a fabricated equal-weight return, and it survived my own testing and two review passes.", "Also in CI: check_syntax.py, and Dependabot on (free on private repos)."]}, {"id": "C1", "epic": "core", "stage": "done", "title": "Themed 6-factor allocation model", "notes": ["Splits the book across the themes, then equal-weights the names inside each theme.", "Scores each theme on a market-cap-weighted blend of 6 factors: PEG, EV/EBITDA, Debt/FCF, P/E (cheaper \u21d2 higher), log average market cap, and price momentum.", "Factor weights are adjustable (default 20/20/20/15/10/15) and re-normalized live.", "A per-theme cap limits concentration; the excess spills to the other themes iteratively."]}, {"id": "C2", "epic": "core", "stage": "done", "title": "Bad / missing data handling", "notes": ["Reported-negative ratios are penalized with a configurable multiple instead of breaking the score.", "Not-applicable metrics (e.g. a bank's EV/EBITDA) carry over a value imputed from the name's quality.", "Missing-but-computable values are derived from the trailing-twelve-month statements.", "Any individual cell can be manually overridden."]}, {"id": "C3", "epic": "core", "stage": "done", "title": "Free live-data layer (Yahoo)", "notes": ["Pulls quotes and fundamentals from Yahoo Finance with no API key.", "Caches results (~15s for quotes, ~6h for fundamentals) and falls back to an embedded seed snapshot if a fetch fails.", "Web fetches through the local Python server proxy; iOS fetches on-device via CapacitorHttp (no CORS)."]}, {"id": "C4", "epic": "core", "stage": "done", "title": "Calculator / Rebalance", "notes": ["Builds the initial portfolio, or rebalances an existing one to the model targets.", "Three modes: full rebalance (buys + sells), cash-only deploy (no sells), and realign-only.", "Self-funding \u2014 the net cash change equals the amount you add."]}, {"id": "C5", "epic": "core", "stage": "done", "title": "Version history (undo / redo / revert)", "notes": ["Saves every build and rebalance as a checkpoint.", "Each checkpoint snapshots holdings, settings, and theme membership together, so undo restores everything in lock-step.", "Supports undo, redo, revert-to-any-point, and git-style forking of the timeline."]}, {"id": "C6", "epic": "core", "stage": "done", "title": "Prices tab", "notes": ["Live prices and day moves for every name, grouped by theme.", "Shows each theme's current value and weight once a portfolio exists."]}, {"id": "C7", "epic": "core", "stage": "done", "title": "Screener tab", "notes": ["Lists the top ~50 stocks by live market cap in each theme.", "Themes are mutually exclusive \u2014 a name shown under one theme never appears under another.", "The universe is curated by tools/build_universe.py."]}, {"id": "C8", "epic": "core", "stage": "done", "title": "Swap a stock in", "notes": ["Tap a screener stock to replace a same-theme holding.", "Triggers a full realign \u2014 sells the removed name to $0 and redeploys its cash to the model.", "Recorded as an undoable checkpoint when you Apply & save."]}, {"id": "C9", "epic": "core", "stage": "done", "title": "Dynamic theme membership", "notes": ["Which tickers belong to each theme is data, not hardcoded.", "Stored in state.themeTickers \u2014 persisted and captured in every version snapshot.", "Read through tickersOf / membership / tradeUniverse helpers; the foundation the E1 epic builds on."]}, {"id": "C10", "epic": "core", "stage": "done", "title": "Cross-platform: web + iOS", "notes": ["One www/ codebase runs as both the web app and a native iOS app (via Capacitor).", "A platform-agnostic data layer talks to the Python server on web, and to Yahoo + localStorage on iOS.", "LAN sync lets the phone share the computer's portfolio.json; a .native CSS skin gives iOS a mobile UI.", "(The iOS build itself is the Mac's domain.)"]}, {"id": "E1.1", "epic": "E1", "stage": "done", "platform": "web", "spec": "docs/features/E1.1_data-driven-themes.md", "title": "Data-driven theme list (foundation)", "notes": ["Makes the set of themes \u2014 which exist, plus each one's key, name and colour \u2014 data (state.themes), not a hardcoded list.", "The whole app now reads the live theme list through new themes() / themeByKey() accessors (14 call sites converted).", "The per-theme cap auto-scales as 1.5 \u00f7 N themes (30% at 5 themes, 50% at 3, ~21% at 7); a cap you set by hand is still honoured.", "The theme list is persisted and saved into every version snapshot, so undo / redo restores it too.", "Invisible at the default 5 themes \u2014 verified byte-for-behaviour identical, and proven to adapt cleanly at 3 and 7 themes.", "Integrated to main 2026-06-26."]}, {"id": "E1.2", "epic": "E1", "stage": "done", "platform": "web", "depends": "E1.1", "spec": "docs/features/E1.2_add-remove-ticker.md", "title": "Add / remove a ticker in a theme", "notes": ["Adds two actions alongside Swap: add a stock to a theme (it grows) or remove one (it shrinks).", "Add is a button in the screener-tap modal; remove is an \u2715 on each holding in the Calculator.", "Both reuse the existing preview \u2192 Apply & save \u2192 version rail, so every change is one undoable checkpoint.", "One theme per ticker: adding a stock that already sits in another theme moves it out of the old one.", "Changing a theme's names re-ranks the whole portfolio (the theme's blended fundamentals shift) and re-sizes every position automatically \u2014 not just the added name.", "Integrated to main 2026-06-26."]}, {"id": "E1.3", "epic": "E1", "stage": "done", "platform": "web", "depends": "E1.2", "spec": "docs/features/E1.3_ticker-search.md", "title": "Ticker search & validate", "notes": ["Search any symbol or company \u2014 incl. names outside the screener (e.g. AMD, TSM, AVGO) \u2014 from the Screener's '\ud83d\udd0d Search' or a theme's '\uff0b name' in Fundamentals.", "Refinement 1: search defaults to Yahoo's live search (reliable; NASDAQ+NYSE+ADRs) \u2014 fixes the 'TSM returns TSMG/TSMU but not TSM' bug, which came from a NASDAQ-only cache (nasdaqtrader's files are now behind a bot wall).", "Refinement 1: optional instant local cache from the SEC's company_tickers.json (10,433 names incl. TSM), opt-in via env PB_SEC_CONTACT=you@email (SEC requires a contact UA; kept out of committed code so the repo stays shareable).", "Refinement 2: a searched name goes to a WATCHLIST at the top of the Screener \u2014 NOT the portfolio, no rebalance. From the watchlist you pick a theme to add it. Excluded from the allocation until added.", "REVISED by E4.3 (2026-07-01): search from Fundamentals now adds a ticker DIRECTLY to that theme + rebalances (a decision); only the Screener search routes to the watchlist, defaulting to NO theme.", "On add, the name's live data is fetched and it's scored like any other (thin data \u2192 existing penalty/compute path).", "Touches server.py (/api/search) + iOS data layer \u2014 Mac syncs both. NOTE: restart a running server to pick up server.py changes (the original 'no matches' was a stale server).", "Integrated to main 2026-06-26 (search + watchlist; TSM fix; opt-in SEC cache)."]}, {"id": "E1.4", "epic": "E1", "stage": "done", "platform": "web", "depends": "E1.1", "spec": "docs/features/E1.4_create-theme.md", "title": "Create a new theme", "notes": ["Create a brand-new theme with a name and a colour via a '\u2795 New theme' button in the Fundamentals tab.", "It appears across every tab and starts empty, ready to fill via Add (E1.2) or Search (E1.3).", "Model rule (built + verified): an empty theme takes 0% of the allocation until it has names \u2014 otherwise it would siphon ~14% of the book into something with nothing to buy.", "Funded themes still sum to 100% and the book fully deploys; with the default 5 (all funded) behaviour is byte-identical (verified maxDiff=0).", "Integrated to main 2026-06-26."]}, {"id": "E1.5", "epic": "E1", "stage": "done", "platform": "web", "depends": "E1.4", "spec": "docs/features/E1.5_rename-recolour.md", "title": "Rename / recolour a theme", "notes": ["Edit an existing theme's display name and colour via a \u270e button next to each theme in the Fundamentals allocation legend.", "Updates consistently everywhere (Prices, allocation legend/bar, screener chips, holdings, history) because E1.1 routes all reads through themes()/themeByKey().", "The theme's KEY never changes on rename \u2014 so membership, the curated Screener list, and past version snapshots stay intact; a pure rename changes no allocations (verified maxDiff=0).", "Built & verified on dev \u2014 awaiting your click-through."]}, {"id": "E1.6", "epic": "E1", "stage": "done", "platform": "web", "depends": "E1.4", "spec": "docs/features/E1.6_delete-theme.md", "title": "Delete a theme / restore defaults", "notes": ["Delete a single theme (\u2715 per theme in the Fundamentals legend) or restore the default 5 (\u21ba button).", "Removing a theme turns its held names into exiting positions \u2014 sold to $0 on the next rebalance; a warning banner in the rebalance preview lists them before any liquidation.", "Deleting your only remaining theme is blocked (use Restore defaults). Note: a true 'zero themes' state isn't meaningful \u2014 the allocator needs \u22651 theme, so 'clear all' is served by Restore defaults.", "Built & verified on dev (delete \u2192 exiting + preview warning, last-theme block, restore to the 5) \u2014 awaiting your click-through."]}, {"id": "E1.7", "epic": "E1", "stage": "done", "platform": "web", "depends": "E1.3", "title": "Similar-stock recommendations (peers of picks)", "notes": ["Recommend names similar to a theme's current picks \u2014 live sector/industry peers via Yahoo.", "Realized by E1.9: dsPeers (Yahoo recommendationsbysymbol) feeds the flat Screener's custom-theme recommendations (e.g. TSM \u2192 AVGO/MU/ASML).", "Integrated to main 2026-06-26."]}, {"id": "E1.8", "epic": "E1", "stage": "done", "platform": "web", "depends": "E1.4", "title": "Seed recommendations from a new theme's name", "notes": ["Use a theme NAME's keywords (e.g. 'Semiconductors') to recommend stocks immediately \u2014 before it has any picks.", "Realized by E1.9: the flat Screener keyword-searches the theme name (+ de-pluralized words) to seed a brand-new theme's recommendations (e.g. Semiconductors \u2192 NXPI/ON/TSM/LSCC).", "Integrated to main 2026-06-26."]}, {"id": "E1.9", "epic": "E1", "stage": "done", "platform": "web", "depends": "E1.3", "spec": "docs/features/E1.9_flat-screener-recommendations.md", "title": "Flat Screener: pooled recommendations + actions", "notes": ["Replaces the 5 per-theme Screener tables with ONE running list: ~15 recommendations per theme (ungrouped) + ALL your holdings, sorted by market cap.", "Custom themes get LIVE recommendations: keyword search on the theme name (+ de-pluralized words) and sector PEERS of the theme's current picks (e.g. TSM \u2192 AVGO/MU/ASML). Original themes use the curated universe.", "Every row is tappable \u2192 add to a theme, swap into a theme, watchlist, or (for a held name) remove it from its theme \u2014 drop a holding with no replacement.", "Delivers the recommendation ideas from E1.7 (peers of picks) and E1.8 (keywords from name); both are realized here and will be marked done on merge.", "New plumbing: /api/peers (Yahoo recommendationsbysymbol) + dsPeers. Integrated to main 2026-06-26 (83-row flat list, custom-theme recs, action sheet).", "REVISED by E4.4 (2026-07-01): the pooled recommendations were removed per user; the Screener is now a search box + the watchlist, and the watchlist 'add to theme' prompts an add-vs-swap confirmation then rebalances. The peers/keyword functions remain in the code (unused)."]}, {"id": "E2.1", "epic": "E2", "stage": "done", "platform": "web", "spec": "docs/features/E2.1_data-driven-metrics.md", "title": "Data-driven metric list (foundation)", "notes": ["Make the SET of scoring metrics data (DEFAULT_METRICS + state.metrics + a metrics() accessor), not the hardcoded 6 \u2014 mirroring E1.1's move for themes.", "computeAllocation loops over metrics(); each descriptor keeps its own score formula so the default 6 stay BYTE-IDENTICAL. Persisted as [{key}] + version-snapshotted; old portfolios migrate.", "Integrated to main 2026-07-01 (byte-identity maxDiff=0 verified)."]}, {"id": "E2.2", "epic": "E2", "stage": "done", "platform": "web", "depends": "E2.1", "spec": "docs/features/E2.2_metric-catalog.md", "title": "Metric catalog + compute layer", "notes": ["METRIC_CATALOG: the 6 defaults VERBATIM + 21 inactive catalog metrics (catMetric()); DEFAULT_METRICS = filter(defaultActive). dirScore/scoreOf; themeMetric null-penalty guard.", "server.py forwards ~21 null-safe Yahoo fields (roe/roa/ps/pb/margins/debtToEquity[%]/current+quick/growth/divYield/payout/beta/forwardPE/evRev + raw fcf/ebitda/revenue/cash/debt).", "Integrated to main 2026-07-01."]}, {"id": "E2.3", "epic": "E2", "stage": "done", "platform": "web", "depends": "E2.2", "spec": "docs/features/E2.3_metric-picker.md", "title": "Choose your metrics (picker UI)", "notes": ["\u270e Choose metrics picker (catalog grouped, coverage badges, >=1 enforced, exactly-default-6 => state.metrics=null).", "Generalized the weights panel, per-theme table (Cap special always-2nd column; other cols loop metrics()), and notes \u2014 all from metrics(). Default-6 DOM byte-identical.", "Integrated to main 2026-07-01."]}, {"id": "E2.4", "epic": "E2", "stage": "done", "platform": "web", "depends": "E2.2", "spec": "docs/features/E2.4_metric-direction-baddata.md", "title": "Per-metric direction & bad-data handling", "notes": ["Group \u2461 -> one row per active metric: AUTO direction (read-only badge, from the catalog map) + policy select offering all 3 (Penalize/Carry over/Ignore) for EVERY metric incl. the 6 + mcap/mom + artificial-value input (lower-better penalize only).", "themeAvgMcap/themeMomentum policy-aware; computeCarryover generalized to impute any carry metric (byte-identical for ev/dfcf). state.metricCfg {key:{penalty?,badData?}} pruned-on-default.", "Integrated to main 2026-07-01 (default-6 byte-identical, git before/after maxDiff=0)."]}, {"id": "E2.5", "epic": "E2", "stage": "done", "platform": "web", "depends": "E2.3", "spec": "docs/features/E2.5_presets-reset.md", "title": "Metric presets / reset to default 6", "notes": ["\u21ba Reset to default 6 (cap NOT reset) + named preset library (save/load/delete). applyConfig replaces weights/penalty/metricCfg wholesale; presets persisted but EXCLUDED from version snapshots.", "Integrated to main 2026-07-01."]}, {"id": "E3.1", "epic": "E3", "stage": "done", "platform": "web", "spec": "docs/features/E3.1_statement-data-layer.md", "title": "Statement fetch + cache (server)", "notes": ["/api/statements route (+ prefetch) over Yahoo's fundamentals-timeseries endpoint (the old v10 quoteSummary statement modules are DEAD \u2014 validated). Same cookie+crumb.", "Fetch ~8 quarters of the 3 statements; normalize to {sym, quarters:[{date, <lineItems>}], asOf}; cache per symbol with a long quarterly TTL + seed fallback. Foundation card.", "Data source + line-item availability validated live 2026-07-13 (AAPL 30/31 keys, NVDA 31/31; banks legitimately lack gross profit / current assets)."]}, {"id": "E3.2", "epic": "E3", "stage": "done", "platform": "web", "depends": "E3.1", "spec": "docs/features/E3.2_computed-metric-engine.md", "title": "Computed-metric engine (client)", "notes": ["Assemble TTM (sum-4Q flows, latest-Q balance) from the loaded statements; compute each E2 catalog metric via a documented formula -> the SAME state.fundamentals[sym].<field> the getters read.", "Forward/market metrics stay pulled (marketCap, price, momentum, forwardPE, peg, beta, divYield). Graceful fallback to the pulled field when a statement input is missing (banks). Per-field source tag.", "Formulas validated vs Yahoo pre-computed (P/E, EV/EBITDA, margins, P/S, P/B, current ratio, D/E match <=3%; ROE/ROA/quick differ by methodology \u2014 transparent single definition is the point)."]}, {"id": "E3.3", "epic": "E3", "stage": "done", "platform": "web", "depends": "E3.2", "spec": "docs/features/E3.3_stock-detail-page.md", "title": "Stock-detail page (UI)", "notes": ["Click a ticker (Prices / Screener / fundamentals table) -> a detail view: the 3 statements x last 4 quarters (+ a TTM column) + a computed-metrics panel (each metric: value + formula + source).", "Forward-looking fields labelled 'pulled, not computed'. Reuse the app's modal/view patterns."]}, {"id": "E3.4", "epic": "E3", "stage": "done", "platform": "web", "depends": "E3.2", "spec": "docs/features/E3.4_source-integration.md", "title": "Integration + source labeling + toggle", "notes": ["Model consumes computed metrics as PRIMARY; a 'compute from statements' toggle (default on) with fallback to pulled; per-metric source tags in the fundamentals table; a compare (computed vs pulled) surface.", "New source => the default allocation shifts vs the pulled path (intended)."]}, {"id": "E3.5", "epic": "E3", "stage": "done", "platform": "web", "depends": "E3.1", "spec": "docs/features/E3.5_performance-ios.md", "title": "Performance + iOS parity", "notes": ["Cache tuning; prefetch holdings' statements; lazy-load Screener names on demand; iOS on-device fetch parity (mirror the ds* pattern). Log any coverage caps.", "iOS note: nativeStatements() mirrors the server for E3, BUT nativeFundamentals() still lacks the E2 extended pulled fields \u2014 see the APP1 card + docs/APP_MIGRATION.md.", "E3.1\u2013E3.5 integrated to main (prod) 2026-07-13."]}, {"id": "E4.1", "epic": "E4", "stage": "done", "platform": "web", "title": "Declutter the allocation-model panel", "notes": ["Top 'Theme allocation model' card keeps only: the user inputs, the % weight summary per theme, and New theme / Restore defaults.", "Three clearly-named input groups by purpose: \u2460 Metrics \u2014 assign weights; \u2461 Exception handling \u2014 the artificial value to use when a metric is negative or not found; \u2462 Maximum weight per theme.", "Spell metrics out fully (e.g. PEG = P/E \u00f7 Growth%, EV/EBITDA = Enterprise Value \u00f7 EBITDA, Debt/FCF = Total Debt \u00f7 Free Cash Flow, P/E, Avg market cap, Price momentum).", "Remove all inline explanations from the top; keep them in the collapsible 'How the math works & data-quality notes' (hidden by default)."]}, {"id": "E4.2", "epic": "E4", "stage": "done", "platform": "web", "depends": "E4.1", "title": "Per-theme actions in each theme's section", "notes": ["Move add-ticker / rename-theme / delete-theme out of the top allocation legend and into each theme's own section header (the #fundGrid cards), next to the theme name.", "The top legend becomes a clean read-only % summary."]}, {"id": "E4.3", "epic": "E4", "stage": "done", "platform": "web", "depends": "E4.2", "title": "Split add flow \u2014 Fundamentals adds directly, Screener watchlists", "notes": ["Adding a ticker from Fundamentals & Allocation is a DECISION \u2192 add it directly to that theme and run the rebalance preview (no watchlist).", "Searching in the Screener is BROWSING \u2192 add the found name to the watchlist, defaulting to NO theme.", "Once a watchlist name is assigned a theme, the user is ready \u2192 trigger the add-to-portfolio + rebalance.", "Revises E1.3 (which currently routes both entry points to the watchlist)."]}, {"id": "E4.4", "epic": "E4", "stage": "done", "platform": "web", "depends": "E4.3", "title": "Screener = search + watchlist; add-vs-swap confirmation", "notes": ["Remove the pooled recommendations list from the Screener (unwanted). Screener = a search box + the watchlist.", "Flow: 1) look up a stock \u2192 add to watchlist (no theme); 2) when decided, choose 'add to theme' \u2192 a confirmation to either (a) add the ticker to the portfolio or (b) swap it for an existing holding; 3) on confirm, run the rebalance.", "Keeps the tap-to-swap capability, integrated into the watchlist commit.", "Revises E1.9 (the flat recommendation Screener)."]}, {"id": "E4.5", "epic": "E4", "stage": "done", "platform": "web", "spec": "docs/features/E4.5_allocation-panel-layout.md", "title": "Allocation-model panel layout", "notes": ["Split the two Fundamentals cards, equal width: LEFT = 'Target vs current weight' (drift table + allocation bar + legend + New theme/Restore + preset row + compute-from-statements toggle/Compare).", "RIGHT = 'Theme allocation model' inputs only: \u2460 Metrics as an aligned name+weight grid, \u2461 Exception handling as an aligned grid, \u2462 Max weight per theme, then the 'How the math works' notes at the bottom.", "\u2461 Exception handling dropped its Direction column to save width \u2014 direction is now a muted inline arrow on the metric name (full text in a hover tooltip); dropped the redundant 'value' label (header already says Value); penalty input got an aria-label.", "Two adversarial reviews folded in (font-size parity; touch/keyboard accessibility). Integrated to main (prod) 2026-07-13."]}, {"id": "APP1", "epic": "APP", "stage": "done", "platform": "ios", "spec": "docs/APP_MIGRATION.md", "title": "Bring the iOS app to parity with web (E1\u2013E4.5)", "notes": ["DONE 2026-07-15 \u2014 merged dev\u2192main (prod). iOS at parity with web E1\u2013E4.5, plus a phone-first native pass this session: (a) Model-tab theme actions now sit on ONE row with full labels (\uff0b Add ticker / \u270e Edit name / \u2715 Delete), left-aligned under the title (pill floated top-right); (b) fixed iOS auto-zoom on every pop-up \u2014 all native form fields pinned to 16px so focusing a field no longer magnifies/mis-places the sheet; (c) Screener Watchlist rebuilt as the scrollable frozen-ticker table (Ticker \u00b7 Company \u00b7 Last \u00b7 Chg% \u00b7 52W High \u00b7 52W Low \u00b7 Volume \u00b7 Avg Vol \u00b7 Add-to-theme \u00b7 remove), which required opting the watchlist table out of the flat-Screener rank-hiding rule + adding 52-week range & volume (regularMarketVolume, averageDailyVolume3Month\u21a610Day) to BOTH server.py fetch_quotes_live AND nativeQuotes. Verified in-browser (web + native-forced), built/run on the iPhone 17 simulator, and \u2014 this session \u2014 INSTALLED & RUNNING on a real physical iPhone (iPhone 16 Pro Max, iOS 26.5.2) via Xcode free personal-team signing, with live on-device Yahoo data confirmed. Free-signing notes: bundle id had to be globally-unique (com.ericwzr.portfoliobuilder123453; generic id failed to make a profile), device must be connected once to mint the profile, Developer Mode ON, trust the cert on first launch, re-run from Xcode ~weekly (in-place update keeps on-device localStorage data). Runtime: data is on-device only (localStorage pb_portfolio_v1; LAN sync optional), no background refresh, free (Yahoo public endpoints). A 4-agent adversarial review of the watchlist diff found no bugs. Ongoing maintenance only = the weekly re-sign + `git pull && npx cap sync ios` before a Run to push a new build.", "IMPLEMENTED 2026-07-15 (on dev; awaiting on-device try + dev\u2192main integration): (1) nativeFundamentals extended with all 21 E2/E3 catalog+market fields \u2014 the critical native gap is CLOSED; (2) fixed a native crash \u2014 CapacitorHttp received numeric params (E3 period1/period2, search counts) and cast-crashed on-device; now stringified in yGet; (3) added native CSS so E3 stock-detail statement tables scroll independently with a frozen 'Line item' column (web unchanged). Built + verified on the iPhone 17 simulator with live on-device data; metric picker / theme editor / stock-detail all render as native bottom sheets. Env: Mac now builds from a git clone OUTSIDE OneDrive (~/Developer/portfolio-builder) \u2014 OneDrive locked the folder from the toolchain; server.py Yahoo TLS fixed via SSL_CERT_FILE=certifi.", "One codebase (www/index.html) wrapped by Capacitor. Most web work reaches iOS just by running `npx cap sync ios` + rebuilding in Xcode \u2014 all UI (E1 themes, E4/E4.5 layout, E2/E3 panels, stock-detail) needs NO native code.", "CRITICAL native gap: nativeFundamentals() (www/index.html ~L884) returns only the 6 original factors; it does NOT mirror the ~21 E2 extended pulled fields server.py returns (forwardPE, evRev, ps, pb, grossMargin/opMargin/netMargin, roe, roa, debtToEquity[%], currentRatio, quickRatio, revGrowth, earnGrowth, divYield, payout, beta + raw fcf/ebitda/revenue/cash/debt). Without this, E2 catalog metrics and the pulled/market metrics are blank on-device.", "Already native: nativeStatements() mirrors server _STMT_FIELDS (E3), plus nativeQuotes/nativeSearch/dsPeers. Verify on-device: E3 compute-from-statements toggle, ADR currency handling, and the stock-detail page.", "Then `npm install` (Node 20) -> `npx cap sync ios` -> open in Xcode -> run; on-device regression of E1\u2013E4.5 + calculator + history + Wi-Fi sync. Full checklist in docs/APP_MIGRATION.md."]}, {"id": "E5.0", "epic": "E5", "stage": "done", "platform": "web", "spec": "docs/features/E5_web-ui-overhaul.md", "title": "App shell \u2014 rail, context bar, theme tokens", "notes": ["Approved from the clickable prototype (www/proto/newui.html): persistent left rail + a sticky context bar that keeps total value / today / invested / max drift on screen on EVERY page (today that context is lost the moment you leave Prices).", "ONE design in TWO themes \u2014 \u2600 Light / \u263e Dark (defaults to the OS setting, remembers the choice). Everything theme-dependent is a CSS token; no layout rule branches on the theme.", "Gotchas already proven in the prototype: transition the `background-color` LONGHAND (the shorthand over a var() sticks at the old colour on a theme swap), and clear the transition-suppression class on a TIMER, not requestAnimationFrame (rAF is paused in background tabs and would disable all motion permanently)."]}, {"id": "E5.1", "epic": "E5", "stage": "done", "platform": "web", "depends": "E5.0", "title": "Overview tab (replaces Prices)", "notes": ["Hero portfolio value + scrubable value-over-time area chart across the version timeline; theme breakdown donut; holdings table with per-row sparklines and live day-change."]}, {"id": "E5.2", "epic": "E5", "stage": "done", "platform": "web", "depends": "E5.0", "title": "Model tab (replaces Fundamentals & Allocation)", "notes": ["The biggest card \u2014 to be split into sub-steps: (a) metric weights + the 27-metric picker + exception handling + cap + presets; (b) targets/drift/allocation visuals + compute-from-statements toggle + source compare; (c) per-theme tables with inline overrides, source tags, per-theme add/rename/delete, and the stock-detail statement page."]}, {"id": "E5.3", "epic": "E5", "stage": "done", "platform": "web", "depends": "E5.0", "title": "Rebalance tab (replaces Calculator)", "notes": ["Initial build, add-cash, full / cash-only / realign modes, the trade plan, and Apply & save as a version checkpoint."]}, {"id": "E5.4", "epic": "E5", "stage": "done", "platform": "web", "depends": "E5.0", "title": "Research tab (replaces Screener)", "notes": ["Ticker search, the watchlist quote table, the per-stock action sheet, and the add-vs-swap confirmation flow."]}, {"id": "E5.5", "epic": "E5", "stage": "done", "platform": "web", "depends": "E5.0", "title": "History tab", "notes": ["Checkpoint timeline, undo / redo / revert-to-here with the fork behaviour, value-at-checkpoint chart, reset portfolio, and the data-sync sheet."]}, {"id": "APP2", "epic": "APP", "stage": "design", "platform": "ios", "depends": "E5.5", "spec": "docs/features/APP2_ios-v2-rebuild.md", "title": "Rebuild the iOS app against the V2 web baseline", "spec2": "docs/V2_WEB_BASELINE.md", "notes": ["DESIGN 2026-08-24: ideation CLOSED, broken into APP2.1-APP2.14. Spec: docs/features/APP2_ios-v2-rebuild.md. Interactive visual reference: docs/prototypes/APP2_phone_demo.html (open on a phone; design reference only - no engine, faked data).", "OWNER DECISION D4 (2026-08-24): the phone uses SUPABASE for account-based data, exactly as web. The native app is therefore ACCOUNT-ONLY: no offline read, STORAGE_ADAPTERS.native (localStorage pb_portfolio_v1) and the LAN Wi-Fi sync are RETIRED, and a mirror must never come back - that hybrid is what produced the ~120 defects of E6-E9. This settles the question V2_WEB_BASELINE.md 9.1 flagged as needing a deliberate answer rather than a copy-paste.", "OWNER DECISION D5 (2026-08-24): UI direction is ROBINHOOD - clean, simple, easy to follow - carrying identical content and features to V2 web.", "OWNER DECISIONS D6-D8 (2026-08-24), all three open questions now CLOSED: (D6) the phone KEEPS FETCHING YAHOO DIRECTLY on-device via CapacitorHttp - it is Yahoo on every platform, since the web app cannot call Yahoo from a browser (no CORS on Yahoo endpoints) and server.py/the Worker are only proxies; the phone is the one platform that CAN call Yahoo directly, and keeping it means the Cloudflare Pages origin being down or drifting cannot stop the phone pricing a portfolio. (D7) phone idle timeout is 15 MINUTES, web stays at 5. (D8) FREE personal-team signing for now, revisit a paid account at the production/testing stage.", "START HERE: docs/V2_WEB_BASELINE.md \u2014 the definition of what the phone must carry. V2 web shipped to main 2026-08-15 (d2b7405); V1, INCLUDING the shipped iPhone app, is superseded.", "DECISION (user, 2026-08-15): the iOS app does NOT have to look like the web app \u2014 platform constraints make a shared pixel design a poor fit \u2014 but it MUST carry the same content and features. Do not reconcile the old .native CSS with V2; it targets V1 markup that no longer exists. Delete it and rebuild from the baseline doc.", "KNOWN + ACCEPTED V1 debt (audited, deliberately not fixed): every `.native .theme \u2026` rule is dead after the accordion rewrite, so on the V1 phone build the metric table loses its frozen ticker column and phone cell density and the theme header cannot wrap; native header/tab bar also hardcode light colours so dark mode is wrong there.", "DECISION (2026-08-08): the iPhone app does NOT have to copy the web UI \u2014 platform/Xcode constraints make a shared pixel-level design a poor fit. It MUST carry the same CONTENT and FEATURES.", "So the shared layer is the engine + data layer + feature set; the presentation layer may legitimately diverge per platform. During E5 the phone keeps its current shipped native UI (the rail/context bar are web-only chrome).", "Do this on the Mac with Xcode after E5 settles: re-verify every E5 capability exists on-device, then redesign the native presentation to suit the phone."]}, {"id": "APP2.1", "epic": "APP", "stage": "done", "platform": "ios", "depends": "APP2", "spec": "docs/features/APP2_ios-v2-rebuild.md", "title": "Strip the dead V1 native skin", "notes": ["Delete all 91 `.native` rules: they target V1 markup (.theme/.head/.lhs/.theme-grid) that the E5 accordion rewrite replaced, so they already match nothing. Owner decision 2026-08-15 was to delete, not repair.", "Tokenise the native header and tab bar - V1 hardcodes light colours there, so dark mode is wrong on the phone.", "This is the clean baseline every other APP2 ticket builds on.", "DONE 2026-08-24. Removed all 90 .native CSS rules; the style block went 639 -> 487 lines. Kept every NATIVE *JS* path untouched (the pre-paint classList.add, the NATIVE constant, useRemote, STORAGE_ADAPTERS.native, the ds* branches) - those are retired later, by APP2.4 and APP2.5.", "PROOF the web is unaffected, not just asserted: web-facing selectors number 293 BEFORE and 293 AFTER, with none lost and none gained; every removed line belonged to a .native rule, its body, or its comment; and no dangling @keyframes remain (pbSheet went with the bottom sheet that was its only user - defined and referenced animation names now match exactly).", "GATES: py -3 tools/check_syntax.py exit 0, final depth 0. npx playwright test 12/12 PASSED, so the engine is provably untouched. npx cap sync ios + xcodebuild = BUILD SUCCEEDED. (Playwright and its chromium had never been installed on this Mac; both are now.)", "EXPECTED INTERMEDIATE STATE: with the skin gone the app renders the WEB layout on the device - desktop rail and all - until APP2.2 builds the new shell. That is the plan, not a regression."]}, {"id": "APP2.2", "epic": "APP", "stage": "done", "platform": "ios", "depends": "APP2.1", "spec": "docs/features/APP2_ios-v2-rebuild.md", "title": "Native shell: tab bar, context bar, tokens, sheets", "notes": ["Bottom tab bar for the five V2 sections (Overview/Model/Rebalance/Research/History); internal view ids stay prices/fundamentals/calc/screener/history.", "Sticky CONTEXT BAR on every screen: Total value / Today / Invested / Gain / Max drift. It must refresh on the PRICE-REFRESH path, not only on structural re-renders - stale money beside live money was a real V2 bug.", "Safe-area insets, bottom sheets, and light+dark through tokens: follow the OS by default, remember an explicit choice, and let no component rule branch on the theme.", "Every form field at least 16px: iOS auto-zooms the whole page below that and throws every sheet oversized and off-screen. Fixed once in V1; must not regress.", "DONE 2026-08-24 (commit 620c3fc). Bottom tab bar with mask-image icons and the V2 intent labels via ::after per data-view, so web markup and its labels are untouched. Sticky context bar on every view. One-line header. Modals as bottom sheets with safe-area padding. Colour comes entirely from the existing V2 tokens, so both themes work with no new palette. THREE bugs found while verifying: the tab bar anchored to the HEADER because backdrop-filter makes an element a containing block for fixed descendants; Rebalance forced its card 133px off-screen because a <select> is as wide as its longest option and a flex item will not shrink below min-content; and the auto-zoom guard had died with the V1 skin, leaving 24 keyboard-raising fields under 16px.", "FOUND during APP2.1: the phone does NOT follow iOS dark mode. Theme init reads const os = !NATIVE && matchMedia(prefers-color-scheme: dark).matches, and the change listener is likewise guarded by !NATIVE - so on the phone os is always false, the app always starts LIGHT, and it never reacts to the OS. This contradicts the baseline rule that themes default to the OS setting. The guards existed because V1 native chrome hardcoded light colours, so dark mode would have looked broken; now that the skin is deleted they are the only thing pinning the phone to light. Remove both !NATIVE guards as part of this ticket and verify dark mode end to end on device."]}, {"id": "APP2.2b", "epic": "APP", "stage": "done", "platform": "ios", "depends": "APP2.2", "spec": "docs/features/APP2_ios-v2-rebuild.md", "title": "In-tab paging for the long tabs", "notes": ["OWNER REQUEST 2026-08-24: on long pages, split a tab into pages you slide between, so each screen carries less - but the workflow must stay coherent, so a user never slides left and right to complete ONE action.", "THE RULE APPLIED: a page split separates READING, never a transaction. The entire rebalance transaction - add capital, mode, Calculate, the trade plan, Apply and save - stays on the Plan page; Current holdings sit on their own page as reference. Anything not listed in a manifest stays visible on EVERY page of its tab, which is how the version banner keeps its undo/redo next to both.", "SPLITS: Overview = Value / Allocation / Holdings. Model = Targets / Model / Themes. Rebalance = Plan / Holdings. History = Timeline / Checkpoints. Research is NOT paged - it already fits one screen at 573px, and search-to-watchlist is a single flow.", "The seams are the desktop two-column boundaries (.ov-left vs .ov-right, .calc-plan vs .calc-holdings, .hist-side vs .hist-list), because those already express the grouping - so no new structure had to be invented.", "MECHANISM: nothing is moved in the DOM. Members of inactive pages are marked .pg-off, so a partial re-render cannot break the structure; a MutationObserver re-tags after any re-render and the pager self-heals. Debounced on a TIMER, not requestAnimationFrame, which is paused in a background tab.", "Navigation is a tap-able segmented bar (always reliable) PLUS swipe. The swipe deliberately refuses any gesture that begins inside something which itself scrolls sideways - the per-theme metric tables and the watchlist - so paging never steals a table scroll. It also ignores gestures starting on inputs, selects, svg charts and sheets.", "RESULT measured on a 402x874 phone: Overview 2221px -> 874px, exactly one screen. Model 2332 -> 1060. Rebalance 1399 -> 874. History 1422 -> 893. No JS errors; engine gate 12/12.", "OWNER-FOUND BUG 2026-08-24 (fixed): the whole screen panned sideways once signed in. Cause: renderAcctBtn turns the account button into an unbounded string - a dot plus the email - and the native control row had been pinned to flex-wrap:nowrap, so .ctrls measured 467px inside a 402px viewport and pushed the page. Fixed by letting .ctrls wrap, giving every child min-width:0, and capping #acctBtn with an ellipsis (the Account sheet still shows the address in full). Added html/body overflow-x:clip as a net - clip rather than hidden, so the sticky header keeps working.", "THE PRINCIPLE the owner stated, now the standing rule: a user must NEVER scroll the whole screen sideways. Only a data table may scroll horizontally, and only within its own card or sheet. Verified: the holdings table now slides inside its card while the page stays put.", "MY VERIFICATION WAS AT FAULT TOO, in three ways, all now fixed in the harness: (1) it ran SIGNED OUT, where the button reads a short Sign in and the overflow cannot appear; (2) it measured documentElement.scrollWidth AFTER body{overflow-x:hidden} had clipped it, so the guard masked the very bug it exists to catch - it now walks the tree for any element wider than the screen that is not inside a legitimate scroller; (3) it measured only page 1 of each tab, and the owner was on page 2 - it now measures every page. MUTATION-TESTED against this defect: with the fix stashed, the harness flags div.ctrls w=467 on 11 of 11 pages."]}, {"id": "APP2.2c", "epic": "APP", "stage": "done", "platform": "ios", "depends": "APP2.2", "spec": "docs/features/APP2_ios-v2-rebuild.md", "title": "Header chrome moves into the Account sheet", "notes": ["OWNER APPROVED 2026-08-24. The phone header keeps only what you act on constantly - the account button and Refresh. The tour button, the light/dark switch and the auto-refresh interval move into the Account sheet under a Settings block.", "They are MOVED, not duplicated. Every one is wired by element, so relocating the real node keeps its handler and its state; a copy would have needed both mirrored, which is exactly the kind of shadow state this project has been burned by.", "closeModal is wrapped on native to return them home BEFORE modalRoot.innerHTML is wiped - otherwise the sheet teardown destroys the relocated controls. That covers every dismissal path, not just the Close button.", "Also gave .ctrls order:-1 so it sits beside the brand rather than being pushed to a third line by the full-width KPI strip.", "VERIFIED: adopt/restore round-trip puts all three back in .ctrls in their ORIGINAL order (syncBadge,tourBtn,acctBtn,themesw,updated,refreshSel,refreshBtn), and still restores when closeModal itself is the dismissal path. Header height 310px -> 92px. 11 pages, 0 overflow; engine gate 12/12."]}, {"id": "APP2.3", "epic": "APP", "stage": "design", "platform": "ios", "depends": "APP2.2", "spec": "docs/features/APP2_ios-v2-rebuild.md", "title": "Account on device: Supabase auth", "notes": ["GoTrue over CapacitorHttp. The web calls Supabase with plain fetch and the publishable key, so it ports directly; only the publishable key ever ships - the secret key carries BYPASSRLS and must never enter the repo.", "The login gate must FAIL CLOSED before any portfolio paint, as web does with its inline pre-paint script.", "Session persistence and token refresh. A TOKEN REFRESH IS NOT AN IDENTITY CHANGE - counting it as one discarded a live portfolio on every stale reload.", "OWNER DECISION D7 (2026-08-24): the phone idle timeout is 15 MINUTES; web stays at 5. Keep a visible warning before it fires, as web does for its last 30 seconds."]}, {"id": "APP2.4", "epic": "APP", "stage": "design", "platform": "ios", "depends": "APP2.3", "spec": "docs/features/APP2_ios-v2-rebuild.md", "title": "Storage: cloud-only on native", "notes": ["Per D4 the account is the ONLY place a portfolio lives. Retire STORAGE_ADAPTERS.native and the LAN Wi-Fi sync on the phone.", "Honour C1-C5: one context captured before the first await; one checkpoint after it; baseRevision is a server-confirmed revision or null and saving is refused while null; nothing about a portfolio is written to the device; a save returns true only if the write landed.", "An unreachable account is REPORTED as unreachable and the user change stays on screen. Never show a cached copy, and DO NOT build a mirror.", "A refusal must come BEFORE the mutation it prevents, and a guard must measure the thing it guards against - both rules were learned from data-loss findings.", "Anything stored per-user on the device is keyed by uid and cleared on sign-out: RLS separates accounts, not devices."]}, {"id": "APP2.5", "epic": "APP", "stage": "design", "platform": "ios", "depends": "APP2.2", "spec": "docs/features/APP2_ios-v2-rebuild.md", "title": "Verify the on-device Yahoo client", "notes": ["OWNER DECISION D6 (2026-08-24): the phone KEEPS fetching Yahoo DIRECTLY on-device via CapacitorHttp, as V1 did. This is a VERIFY ticket, not a rewrite.", "The framing that matters: it is Yahoo Finance on every platform. The web app cannot call Yahoo from a browser because Yahoo sends no CORS headers - that is the entire reason server.py (dev) and worker/src/index.js (prod) exist, as proxies fetching Yahoo on its behalf. The phone is the one platform that CAN call Yahoo directly, because CapacitorHttp is native HTTP and is not subject to CORS.", "Why keep it: no dependency on the Cloudflare Pages origin. Under D4 the phone needs the network for the portfolio, but that is SUPABASE - so Pages being down, renamed or drifting cannot stop the phone pricing a portfolio. And V1 already proved this path on a physical iPhone with live data, so it is zero new code.", "Accepted cost: three implementations of one Yahoo surface stay in sync - server.py, the Worker, and the native client. E11.2b confirmed the native JS client ALREADY mirrors server.py exactly. Police any future field change with py -3 tools/diff_proxy.py, and remember that porting logic ports its bugs (the search tie-break was fixed in Python and reintroduced identically in JS the same afternoon).", "OWNER CAVEAT, answered: does calling Yahoo directly risk a lockout? The evidence says direct is the SAFER side. E11.0 existed to test the OTHER direction and recorded that Yahoo deployed TLS fingerprinting in Apr 2025 and a WORKER cannot control its TLS fingerprint - a datacenter IP with an uncontrollable fingerprint, versus a real iPhone on a residential/carrier IP presenting a stock iOS fingerprint. A proxy also CONCENTRATES all users onto a few shared Cloudflare IPs, so one throttle breaks everyone; a device distributes the load and looks like a person browsing Yahoo. Measured 2026-08-24 from a residential IP through server.py, the same call pattern the phone uses: 100 Yahoo calls in about 4 seconds (forced 25-name fundamentals x3 plus 25-name statements) returned 25/25 live every run with ZERO throttle signals. Y_CONCURRENCY is capped at 4, so the phone never bursts harder. V1 already ran this path on a physical iPhone with live data.", "FALLBACK if Yahoo ever does lock out direct device calls: this is not a one-way bet. The Worker keeps existing for the web app and the ds* layer already branches, so pointing the phone at the deployed /api/* is a small localised change. APP2.14 must watch for throttling during on-device verification. Degradation is already graceful - the f4eb179 hardening rejects an error body as a crumb, forces a crumb refresh, and falls back to seed rather than crashing.", "Scope: confirm nativeQuotes/nativeFundamentals/nativeStatements/nativeSearch still match field-for-field on device; keep EVERY CapacitorHttp param stringified (numbers cast-crash NSCFNumber to NSString - the V1 crash); keep search on the local ticker directory www/data/tickers.json, which is client-side and needs no proxy on either platform; and confirm quoteType still arrives so the ETF never-a-theme-member guard holds."]}, {"id": "APP2.6", "epic": "APP", "stage": "done", "platform": "ios", "depends": "APP2.2", "spec": "docs/features/APP2_ios-v2-rebuild.md", "title": "Overview view", "notes": ["Hero value, today move, all-time gain vs invested.", "Value chart across every checkpoint from each version RECORDED valueAfter (never re-derived), with the Invested reference and the dashed equal-weight counterfactual. valueHistory() must respect the version HEAD: restoreVersion moves head but never truncates versions, so plotting the whole array charts undone checkpoints.", "V2 two chart cards (value, and the $ / % return chart) collapse into ONE segmented chart - Value / Return % / Return $ - which keeps every feature and suits a phone. Legend and the honest note line are kept.", "Allocation donut with a per-theme legend showing current %, target and drift pp. NEVER renormalise to the themed subtotal: value held outside a theme is an explicit Exiting / unthemed segment.", "Four stat tiles, then holdings-by-theme collapsed by default, each showing % of book / Market value / Cost basis / Profit / loss / Return SPELLED OUT (the owner rejected abbreviations), expanding to per-ticker price, day change and value."]}, {"id": "APP2.7", "epic": "APP", "stage": "design", "platform": "ios", "depends": "APP2.2", "spec": "docs/features/APP2_ios-v2-rebuild.md", "title": "Model view", "notes": ["Drift table (theme / target / current / drift pp) and the stacked allocation bar with its cap annotations.", "The model: 1 metrics and weights, 2 exception handling per metric (penalize / carry-over / ignore plus penalty value), 3 maximum weight per theme. Compute-from-statements toggle (default on), Compare sources, and the coverage note. Presets: save / load / delete / reset-to-default-6.", "Per-theme metric tables: 7 columns at defaults, up to 27 fully loaded. Horizontal scroll with a FROZEN TICKER COLUMN - the V1 pattern the owner explicitly asked to keep. Inline click-to-edit overrides with reset-to-live and the prem / qual / calc / ovr / na / live / seed tags.", "Metric picker (6 of 27 active) as a bottom sheet. Per-theme add ticker / rename and recolour / delete, and create theme. There is NO restore-defaults: E8 deleted default themes and that button, so do not rebuild noDefaults.", "Accordion open state lives in module state so a background refresh never collapses what the user opened, and dataset keys round-trip as strings so they must be compared as strings."]}, {"id": "APP2.8", "epic": "APP", "stage": "design", "platform": "ios", "depends": "APP2.2", "spec": "docs/features/APP2_ios-v2-rebuild.md", "title": "Rebalance view", "notes": ["First-run build flow (starting capital, then build initial portfolio) with its preview table, and the banner for a portfolio whose build was undone.", "Add capital; modes full rebalance and new-cash-only; realign-only with no new money; the trade plan as a by-theme table plus per-stock instructions with buy/sell badges, amount, shares, price and value before to after; the amber warning when names will be sold to zero.", "APPLY AND SAVE must never be buried in a nested scroll - dock it as an opaque action bar. A translucent sticky button reads as broken overlap; that was found in the prototype.", "Current holdings with per-name remove, cost basis, unrealised gain and the portfolio start date. Applying creates a version checkpoint."]}, {"id": "APP2.9", "epic": "APP", "stage": "design", "platform": "ios", "depends": "APP2.2, APP2.5", "spec": "docs/features/APP2_ios-v2-rebuild.md", "title": "Research view", "notes": ["Inline local search over the bundled ticker directory: debounced, and the STALE-RESPONSE GUARD is kept so a newer keystroke supersedes an older result.", "Watchlist quote table: last, change %, 52-week high and low, volume, average volume. The watchlist never touches the portfolio.", "From the watchlist: pick a theme, then add as a new name OR swap for a holding, then review the rebalance before saving.", "ETFs are researchable but can NEVER join a theme (E11.9). The guard sits above the first write in setMembership, because a refusal that has already mutated is not a refusal."]}, {"id": "APP2.10", "epic": "APP", "stage": "design", "platform": "ios", "depends": "APP2.2, APP2.4", "spec": "docs/features/APP2_ios-v2-rebuild.md", "title": "History view + Account card", "notes": ["Value-per-checkpoint chart, and every build or rebalance as a checkpoint: date, mode, cash added, trade count, value before to after with the delta, the per-theme allocation and the full trade list.", "Undo / redo / revert-to-any-point with git-style forking - applying from a reverted point drops the redo future, and redoable futures are visually distinct. Rolling 10-checkpoint retention (E10).", "Clear chart scrub handlers when the chart empties, or a reset portfolio still reports its old dollars.", "Replace the V1 Wi-Fi data-sync card with an ACCOUNT card: signed in as, portfolio stored in your account only, export my data, sign out. The danger zone keeps Reset portfolio."]}, {"id": "APP2.11", "epic": "APP", "stage": "done", "platform": "ios", "depends": "APP2.6, APP2.10", "spec": "docs/features/APP2_ios-v2-rebuild.md", "title": "Touch pass", "notes": ["Chart scrubbing is MOUSE-ONLY on web (onmousemove / onmouseleave; there is no touchstart, touchmove or pointermove anywhere in www/index.html). The scrub is the only way to read any checkpoint but the last, so on a phone that data is unreachable today. Replace with pointer events.", "Convert pointer maths through getScreenCTM(): an svg viewBox whose aspect differs from its CSS box is letterboxed, and bounding-rect maths goes out of register.", "No information may live only in a title attribute or a hover state - the donut segments, the history bars and 48 other title attributes all need a tap affordance.", "Tap-target audit at 44pt minimum, and check the keydown trap: a container handler calling preventDefault() swallows activation of any button inside it unless it guards on e.target.closest.", "DONE 2026-08-24 (chart scrubbing). wireScrub() mirrors the existing onmousemove/onmouseleave onto pointer events for #ovChart and #ovRetChart. Assigned as PROPERTIES, not addEventListener, because a chart re-renders on every refresh and listeners would stack; mouse pointers are skipped so the existing handler cannot fire twice. Both teardown sites now call clearScrub() as well, or a reset portfolio would keep reporting its old dollars through the touch path.", "CSS gives the charts touch-action:pan-y, so a vertical drag still scrolls the page while a horizontal one scrubs - and the APP2.2b pager already refuses swipes beginning on an svg, so scrubbing can never flip the page.", "VERIFIED functionally, not just wired: a synthetic touch drag moved the hero from the live $92,000 to $89,100 / 2026-04-30 / cash deploy / invested $85,000, and releasing returned it to the default. The letterboxing trap was already handled - both scrubs convert through getScreenCTM().", "NOT a gap after all: the donut and the history bars carry <title> tooltips, but neither is the only source of that data - the donut legend prints current %, target and drift as text, and the checkpoint list prints every date, value and delta. So no information is hover-only. Remaining under this ticket: the 44pt tap-target audit."]}, {"id": "APP2.12", "epic": "APP", "stage": "design", "platform": "ios", "depends": "APP2.7", "spec": "docs/features/APP2_ios-v2-rebuild.md", "title": "Stock-detail sheet", "notes": ["Ticker click-through from any view: header with price, market cap, TTM as-of, the computed-versus-pulled note and the ADR currency-mismatch tag.", "The 27-metric grid, each tile showing value, formula and source tag.", "Three financial statements across recent quarters plus TTM, each table scrolling independently with a FROZEN line-item column. That was the V1 fix and must be rebuilt against V2 markup."]}, {"id": "APP2.13", "epic": "APP", "stage": "design", "platform": "ios", "depends": "APP2.6, APP2.7, APP2.8, APP2.9, APP2.10", "spec": "docs/features/APP2_ios-v2-rebuild.md", "title": "Signing and distribution", "notes": ["OWNER DECISION D8 (2026-08-24): stay on the FREE personal team for now, accepting 7-day certs and a weekly Xcode re-run. Upgrade to a paid Apple Developer account ($99/yr: year-long certs plus TestFlight, no cable) at the production/testing stage. Note that under D4 a re-run no longer needs to preserve anything on the device, because the portfolio lives in the account.", "Known-good setup from APP1: the bundle id must be globally unique for a free team (com.ericwzr.portfoliobuilder123453, team 5F48K52NKV); a device must be connected once to mint a provisioning profile; Developer Mode on; trust the cert on first launch; and SKIP the Xcode Update to recommended settings prompt, because its User Script Sandboxing breaks the CocoaPods build. Full walkthrough in docs/IOS_BUILD.md.", "CocoaPods needs LANG and LC_ALL set to en_US.UTF-8 or pod install throws a Unicode ASCII-8BIT error. Build from the clone OUTSIDE OneDrive."]}, {"id": "APP2.14", "epic": "APP", "stage": "design", "platform": "ios", "depends": "APP2.11, APP2.12, APP2.13", "spec": "docs/features/APP2_ios-v2-rebuild.md", "title": "On-device verification", "notes": ["Verify every item of the feature contract on a physical iPhone with live data - the same discipline APP1 used, which is how the CapacitorHttp numeric-param crash and the auto-zoom bug were both found.", "Regression: theme CRUD, metric overrides, build then rebalance then apply, history undo / redo / revert forking, watchlist add and swap, ETF rejection, and both themes.", "Account isolation on the device: sign-out clears everything keyed by uid, and a second account sees only its own portfolio.", "Then APP2 to done, and update docs/APP_MIGRATION.md plus V2_WEB_BASELINE.md 9.1 to record the account-only outcome."]}, {"id": "E5.6", "epic": "E5", "stage": "done", "platform": "web", "title": "Equal-weight benchmark + return chart", "notes": ["Overview value chart: 'Value' renamed 'Portfolio Value', the 'Invested' line removed, and a new 'Equal-weight Strategy Value' line added.", "Equal-weight counterfactual = same names, same cash, same dates, same RECORDED trade prices \u2014 but every holding weighted equally. Initial capital is split equally; at EVERY checkpoint (with or without new cash) the whole book is redistributed to exactly equal weight; between checkpoints it drifts with prices, mirroring the real portfolio. It therefore isolates the WEIGHTING model from selection and timing.", "Second chart: return vs invested for both strategies, with a % / $ toggle. Sanity-checked: at the INITIAL checkpoint both strategies read exactly the capital deployed and 0.00% return.", "Data is fully grounded \u2014 price coverage verified at 100% of held names across every checkpoint, so nothing is estimated."]}, {"id": "E6.0", "epic": "E6", "stage": "ideation", "platform": "web + ios", "spec": "docs/features/E6_multi-user-platform.md", "title": "Keep Phase 1 on the multi-user path (no build)", "notes": ["ROADMAP (2026-08-08): Phase 1 = solo sandbox, everything free (WE ARE HERE). Phase 2 = me + a handful of INVITED friends for feedback, centralised + secured + per-account, still free tier. Phase 3 = commercial launch, then discuss paid/licensed data APIs.", "Today web and iOS CAN already share one book via opt-in LAN sync (useRemote -> the web's portfolio.json over Wi-Fi), but with no account, no password, no encryption, same-network only. Fine for one person; not a basis for Phase 2.", "Cheap do-now items that cost nothing and prevent rework: keep the storage seam pure (UI never touches localStorage/fetch directly - an E5 invariant); add schemaVersion; add a monotonic revision + updatedAt on save; keep the engine free of I/O.", "NOT being built now. No accounts, no backend, no database, no paid services in Phase 1."]}, {"id": "E6.1", "epic": "E6", "stage": "done", "platform": "web", "depends": "E6.0", "spec": "docs/features/E6_database_design.md", "title": "Forward-compat: schemaVersion + revision + updatedAt", "notes": ["Add schemaVersion, a MONOTONIC revision, and updatedAt to the saved portfolio document. Local only - no backend, no vendor decision.", "revision is the optimistic-concurrency token E6.5 needs; schemaVersion makes any future server-side migration mechanical.", "Deliberately kept OUT of E5 so savePortfolio stayed byte-identical to prod for a provably safe merge. Do this FIRST."]}, {"id": "E6.2", "epic": "E6", "stage": "done", "platform": "web", "depends": "E6.1", "spec": "docs/features/E6_database_design.md", "title": "Extract a storage-adapter interface", "notes": ["Formalise the existing loadPortfolio/savePortfolio seam into a named adapter interface so the cloud backend becomes a 4th implementation alongside web-file / iOS-localStorage / LAN-sync.", "No behaviour change and no vendor decision - a pure refactor, verified by the app behaving identically."]}, {"id": "E6.3", "epic": "E6", "stage": "done", "platform": "web", "depends": "E6.2", "spec": "docs/features/E6_database_design.md", "title": "Choose provider; stand up schema + row-level security", "notes": ["NEEDS USER INPUT (design doc S9): managed backend-as-a-service (recommended) vs self-hosting server.py + Postgres; acceptance that a public anon key in the client ends the 'no API keys' character; which region the data may live in; invite-only vs open registration.", "Schema: one row per user - user_id, schema_version, revision, updated_at, data JSONB (exactly today's portfolio.json shape); RLS policy scoping every read/write to auth.uid().", "Document-not-normalised on purpose: the version timeline (undo/redo/fork) is the trickiest logic in the app and must not be re-expressed as rows in the same step that introduces auth."]}, {"id": "E6.4", "epic": "E6", "stage": "done", "platform": "web", "depends": "E6.3", "spec": "docs/features/E6_database_design.md", "title": "Login / register / logout + session handling", "notes": ["First real UI addition since E5. Use the provider's auth - never hand-roll password storage, reset or lockout.", "Sign-out now clears every trace of the account (portfolio, revision, themes, watchlist, metrics, presets, weights, penalty, cap) - without it a different, empty account was offered the previous user's book to import. Verified with per-account markers across repeated sign-in cycles.", "A session that dies mid-use (revoked/expired refresh token, paused project) now re-gates the app. It used to keep running unauthenticated: the adapter fell through to the identity-free store and the next save wrote the account's portfolio over the shared server file."]}, {"id": "E6.5", "epic": "E6", "stage": "done", "platform": "web", "depends": "E6.4", "spec": "docs/features/E6_database_design.md", "title": "Cloud adapter with optimistic concurrency", "notes": ["Every save carries the revision it was based on; the server updates only if it still matches, else 409 Conflict.", "On conflict, reload the server copy and tell the user plainly - financial records must NEVER be auto-merged.", "Keep a local mirror after every successful save (the iOS LAN-sync adapter already does this) so offline reads work and the cloud is not a single point of failure.", "HARDENED over three review rounds. baseRevision holds only a SERVER-CONFIRMED revision (null means saving is refused); the offline mirror moved off STORE_KEY to pb_cloud_mirror_<uid>; savePortfolio() returns true/false; kept-aside work is stashed AND readable back. Contract: E6_database_design.md sections 16-17.", "Stashing decides by CONTENT, not revision number: two devices editing from the same base both stamp base+1, so the old 'mirror revision is not ahead' test discarded genuine offline work. Verified live: divergent edits at equal revisions are kept, distinct edits both survive, identical repeats dedupe.", "ROUND 5 (2026-08-15) = NOT CONVERGED: 3 defects that lose data or cross accounts, 2 of them regressions from round 4's own fixes. Fixed but NOT yet re-verified. (a) loadPortfolio's FAILURE branch had no session guard, so a stalled request from a signed-out user hydrated that account's mirror into the next user's session; (b) dropMirror ignored stashUnsyncedMirror's 'failed' sentinel and deleted the only copy of an unsaved edit; (c) 'Start fresh' resolved its modal before its own INSERT, letting a stash restore interleave and drop a document that was never written.", "DO NOT MERGE until a sixth review round comes back converged. Findings per round so far: 36, 30, 19, 6, 3 - and EVERY round has contained defects introduced by the previous round's fix. The invariants and the failure that motivated each one are in E6_database_design.md sections 16-18.", "ROUND 6 (2026-08-15) = NOT CONVERGED. The critical one was a guard measuring the wrong thing: sbEpoch bumped on EVERY session write, and a token refresh is one - so on the commonest path in the app (any reload more than an hour after signing in) the 401 -> refresh -> retry succeeded and then stale() threw the recovered portfolio away. The user saw 'No portfolio yet' over a live account and their first click blanked the offline mirror. Now the epoch tracks USER IDENTITY only. Verified live: expired-token reload restores 25 holdings at revision 19, mirror intact.", "ROUND 7 (2026-08-15) = NOT CONVERGED: 5 findings, 2 again regressions from round 6. (a) HIGH - the epoch guard round 6 put on the mirror write meant a session REJECTED DURING a save skipped the rescue entirely, so the commonest way to lose a session also lost the edit while the toast said it was kept; now the work goes to the owner's stash without resurrecting the plaintext mirror. (b) MEDIUM - 'dirty' and 'could not be filed' were the same bit, so an ordinary failed-then-retried save filed its own ancestor as unsaved work and restoring it reverted the account; they are now separate bits. Plus: a restored stash is consumed by the first successful save, and nativeSavePortfolio now throws instead of reporting success on a full localStorage.", "E8 REWRITE (2026-08-15): after 7 patch rounds and ~96 defects with a regression rate that never fell, loadPortfolio/_savePortfolioInner/the cloud adapter were rewritten around ONE context and ONE checkpoint (contract C1-C5, stated once). Six audits of the rewrite found 7/6/4/1/5/6 - the core loop held throughout; almost every finding was in the kept-aside recovery subsystem.", "E9 (2026-08-15, OWNER'S DECISION): NOTHING about a portfolio is written to the device. Deleted the mirror, the dirty/unfiled bits, the unsynced/conflict stashes, the recovery prompt and 11 functions. A save that cannot reach the account did not happen and says so; an unreachable account is reported, not replaced by a cached copy. purgeLegacyLocalCopies() clears anything earlier versions left behind. COST: reverses E6.8's 'a stored session keeps working while the backend is paused' - during a free-tier pause the app cannot show the portfolio."]}, {"id": "E6.6", "epic": "E6", "stage": "done", "platform": "web", "depends": "E6.5", "spec": "docs/features/E6_database_design.md", "title": "Import local portfolio.json + cutover", "notes": ["On first login with an empty account, offer to import the local file as-is. Keep portfolio.json on disk untouched as the pre-migration backup.", "Import re-checks for an existing row, hydrates through the SAME path a normal load uses (it previously uploaded a book stripped of themes, watchlist, metrics, presets, cap, weights and penalty), and fails loudly instead of reporting success. 'Empty account' now means no row - not a holdings count of zero.", "Cross-user guards verified as a truth table: claimed by another account, not offered; account read failed, not offered; clean and unclaimed, offered; claimed by me, offered. pb_portfolio_v1 stayed byte-identical (18,871 bytes) through every test."]}, {"id": "E6.8", "epic": "E6", "stage": "done", "platform": "web", "depends": "E6.4", "spec": "docs/features/E6_database_design.md", "title": "Login landing page - sign-in required before anything", "notes": ["USER REQUEST (2026-08-15): a proper log-in landing page. Nobody reaches the app without signing in first.", "This CHANGES THE APP'S CHARACTER: today it runs fine signed-out on local storage. With a hard gate, no account = no app, and the local portfolio.json path becomes reachable only through the E6.6 import.", "OPERATIONAL RISK to design around: the free tier PAUSES after a week idle. A naive gate would lock the user out of their own portfolio whenever the backend is asleep or offline. So: a VALID STORED SESSION must still open the app against the local mirror when the backend is unreachable \u2014 the gate blocks strangers, it must not block the owner during an outage.", "Sign out returns to the landing page.", "The gate is visible in markup and taken down pre-paint only when a stored session exists, so the page fails closed if a boot fetch hangs. It lifts only AFTER the account's data has loaded - previously a new user saw the previous user's holdings for the seconds boot spent on the network.", "init() and the sign-in path now share one boot function; they had drifted, so reloading the page skipped the sync badge, the import offer and the kept-aside-work prompt."]}, {"id": "E6.7", "epic": "E6", "stage": "integration", "platform": "web", "depends": "E6.6", "spec": "docs/features/E6_database_design.md", "title": "Security review - GATE before inviting anyone", "notes": ["Prove isolation with a SECOND account: cross-user read/write must fail at the DATABASE, not just in the UI.", "Confirm no service key ships in the client bundle, TLS is enforced end to end, and delete-account removes the row.", "No tester is invited until this passes.", "THREE adversarial review rounds over E6.4-E6.8. Round 1: 36 confirmed. Round 2: 30 more, including three HIGH data-loss paths INTRODUCED by the round-1 fixes (lockOut cleared the very state its own save guards read). Engine re-verified untouched after every round - 28 functions byte-identical to main.", "Proven live, not by inspection: a session dying mid-save with another save queued behind it writes NOTHING to the shared /api/portfolio (0 writes; portfolio.dev.json byte-identical) and both edits are rescued; a forged session with no refresh token cannot pass the gate or reach the offline mirror.", "OUTSTANDING, needs the dashboard: (1) check 6, confirm account deletion cascades; (2) custom SMTP before ANY invite (the built-in sender is capped at 2 emails/hour); (3) flip signup to invite-only. See sql/ISOLATION_TEST.md.", "Round 5 verdict: NOT CONVERGED. The gate for inviting anyone is therefore still CLOSED, independently of the three outstanding Supabase dashboard actions.", "Check 6 is now PARTLY evidenced: the owner registered a real account with email confirmation, deleted it, and the DB shows the account gone with ZERO orphaned rows. Still not decisive - both remaining rows belong to the two test accounts, so we cannot tell whether the deleted account ever had one. To close it: delete test-b (confirmed to hold a row, revision 2 / 4 checkpoints) and re-run the count - portfolio_rows must drop 2 -> 1. See sql/ISOLATION_TEST.md.", "CHECK 6 CLOSED 2026-08-15: the owner listed the rows (test-a rev 19 / 3 checkpoints, test-b rev 2 / 4 checkpoints), deleted test-b, and portfolio_rows dropped 2 -> 1 with orphaned_rows still 0. That is on-delete-cascade firing on a row KNOWN to exist, not a vacuous zero. **The isolation gate is now 8 of 8.**", "NOTE: test-b@example.com no longer exists. Any future cross-account test needs a second account created from the dashboard (the assistant does not create accounts). test-a@example.com / Test@1234 survives.", "REMAINING before inviting anyone: custom SMTP (built-in sender is capped at 2 emails/hour, so invites and password resets stall) and flipping signup to invite-only (currently OPEN - the owner's own registration with a real email confirmed that).", "PRE-TESTING (owner, 2026-08-15): move Supabase to a PAID tier before formal testing. Free projects pause after a week idle and, since E9 removed every local copy, a paused project means testers see nothing rather than a cached portfolio. This retires the old E6.8 requirement that a stored session must keep working while the backend is asleep. Remaining pre-invite items: custom SMTP (built-in sender is 2 emails/hour) and invite-only signup.", "2026-08-16: ALL E6-E10 CODE IS NOW MERGED TO main/prod. This ticket stays OPEN anyway, because it is a GATE on inviting people, not on shipping code, and all three owner-only actions (paid tier, custom SMTP, invite-only signup) are still outstanding. Signup is OPEN to anyone with the URL right now. The assistant cannot do any of the three - they are Supabase dashboard actions on the owner's account.", "OWNER DECISION 2026-08-16 - all three DEFERRED, we are not in the testing phase yet: open signup is acceptable for now; custom SMTP and the paid tier will both be resolved when we move to paid at testing time. Interim plan for pausing: resume the project manually from the dashboard (Free projects pause after 7 days of low activity, restorable for up to 1 year - Dashboard > organization > project > Resume project). Better still, simply USING the app once a week is the activity that prevents the pause. This ticket stays open as the reminder, not because anything is broken.", "ROUND 6 (2026-08-27) - the first pass over the CLIENT surface; rounds 1-5 were the data layer. Found a real stored XSS: the app had NO escaping helper at all and 49 template interpolations dropped free text straight into innerHTML. PROVEN by executing it, not by reading: a theme name of <img src=x onerror=...> fired SEVEN times per render, and the Supabase session token sits in localStorage where any injected script reads it - so XSS meant account takeover.", "Severity, stated honestly: in the normal flow it is SELF-XSS - you have to type the payload into your own theme name, and no path shows one account's strings to another (no sharing, no file import, feedback has no select policy). The latent third-party path is the ticker directory: build_ticker_directory.py does no validation and the weekly bot refreshes it, so one upstream row containing < would have reached innerHTML in every user's browser. Today's data is clean - 0 angle brackets, 0 double quotes across 11,317 entries.", "Fixed at the SINK with esc(), not on input - input-escaping corrupts the stored value and double-escapes on every re-save. tests/security.spec.js proves both halves: hostile markup is inert AND ordinary names like 'Robotics & AI' still read back exactly, because an over-eager escape showing users '&amp;' is also a defect. Two of the five fail against the pre-fix code.", "Because escaping is now at every sink, input validation in the ticker builder is NOT needed - it would be a second guard on a closed hole.", "Added www/_headers: the app had NO CSP and NO X-Frame-Options, so it was framable (clickjacking against a signed-in session that can delete its own account). CSP keeps script-src 'unsafe-inline' deliberately - the app is three inline scripts and a stale per-deploy hash would block every one of them, taking the whole app down. So it does not stop injected script RUNNING; it stops it REACHING an attacker (connect-src, img-src, form-action, base-uri, object-src). Honest limit: CSP cannot block top-level navigation, so location=... exfiltration would still work. Verified before deploy by replaying the real policy locally over every view plus the tour and Account sheet: 0 violations, 0 page errors.", "check_syntax.py now fails if the CSP's connect-src does not name the SB_URL the app actually calls - a drifted origin would break every sign-in as an opaque console block. Mutation-tested.", "RE-VERIFIED LIVE against prod, not by inspection: anonymous SELECT on portfolios and an anonymous call to delete_own_account are BOTH denied with 42501, at the GRANT level, before RLS is even consulted. The SQL is textbook - FORCE row level security, explicit auth.uid() is not null, to authenticated, and SECURITY DEFINER with search_path pinned to '' and the uid taken from auth.uid() rather than an argument.", "No SSRF in the proxy: every upstream URL is a hardcoded Yahoo literal and symbols are encodeURIComponent'd, so /api cannot be aimed at another host. It IS an open unauthenticated Yahoo proxy with no rate limit or Origin check - an abuse and quota problem, not a pivot.", "STILL OPEN, so THE GATE REMAINS CLOSED: (1) custom SMTP - measured this session, mailer_autoconfirm is false and disable_signup is false, so a stranger can start signing up and can never finish. RESOLVED SAME DAY: (2) the Auth redirect allowlist - the owner set it and it is verified, our prod URL now round-trips AND a nested path /reset/x survives, which is what the double asterisk buys, while a non-allowlisted URL falls back to the prod Site URL instead of localhost; (3) pb-proxy deleted with the owner's go-ahead - it now 404s while prod /api still returns live quotes, and worker/wrangler.toml is annotated and set workers_dev=false so an accidental deploy cannot hand out a public URL again. ACCEPTED, not fixed: (4) no size cap on a stored portfolio, since E10.1 bounds history at 10 versions.", "MOBILE WEB (2026-08-27), found while hardening the demo path: tools/phone/ only ever verified the CAPACITOR build - it stubs window.Capacitor so NATIVE is true. The owner's actual demo is the web app in Mobile Safari, where NATIVE is false, and nothing was watching it. Two defects sat on the first screen a new user sees: nav.tabs still carried the pre-E5 labels (Fundamentals & Allocation, Calculator / Rebalance) so 419px of text in a 390px viewport pushed History off screen and panned the whole page, breaking the owner's own standing rule; and the gate's email and password fields were 14px, which makes iOS Safari zoom the page on the first tap. Same root cause both times - a fact taught to one reader of it: the rail and the native tab bar were renamed and nav.tabs was not, and the 16px rule was written '.native input...' so the browser that actually zooms was never covered. tests/mobileweb.spec.js guards both plus label parity between the strip and the rail; all five fail against the pre-fix code. Header also trimmed 253px to 183px on a 390px screen."]}, {"id": "E7.1", "epic": "E7", "stage": "done", "platform": "web", "depends": "E6.6", "spec": "docs/features/E7_first-run-onboarding.md", "title": "Start fresh means a genuinely empty board", "notes": ["USER-FOUND (2026-08-15): choosing Start fresh still showed the five built-in themes with no stocks in them, and a donut of target weights computed over themes the user never chose.", "Cause: state.themes=null MEANS 'use the defaults', and Start fresh left it null. Fixed with an explicit, persisted noDefaults flag rather than by changing what null means - every existing portfolio relies on that meaning, so no existing document changes behaviour.", "Empty states added: Model shows 'No themes yet' with Create your first theme / Use the built-in 5 / Show me how; Rebalance says 'Build your themes first'; the context bar points at Model instead of Rebalance; the donut already said 'No themes yet'. defaultCap() guards the 1.5/N division against zero themes.", "Start fresh now lands the user on Model with the guide open, because that is where a portfolio actually starts.", "E8: there are no default themes at all now (owner's decision) - the 5 built-ins were a single-user legacy. themes() is simply the list; 'Restore default 5' is deleted; deleting your last theme is allowed. Pre-E8 documents are migrated once, keyed on schemaVersion, handling the theme list and the membership independently (they were separate fallbacks) and covering version snapshots."]}, {"id": "E7.2", "epic": "E7", "stage": "done", "platform": "web", "depends": "E7.1", "spec": "docs/features/E7_first-run-onboarding.md", "title": "Guided tour - one per view", "notes": ["USER REQUEST (2026-08-15): floating-bubble prompts that walk a first-time user through creating their first portfolio, one guide per page.", "Five tours: Model (7 steps: create a theme, add tickers, choose metrics, weight them, bad-data policy, cap, targets), Overview (4), Rebalance (4), Research (2), History (2).", "Anchored to real controls. The ring is drawn by an OVERLAY, not by restyling the target - restyling would move the very element being pointed at. Steps whose anchor is not on screen are skipped, so a tour written for a full board still reads correctly on an empty one.", "Shown once per ACCOUNT per view (pb_tour_<uid>_<view>), so a second person on the same browser gets their own walkthrough. Replay any time from the ? button in the header. Switching views ends the tour.", "Verified: all five run end to end, bubbles stay on screen, rings anchor, full teardown with no leftover DOM, seen tours do not reappear, a fresh user auto-opens.", "FIRST REVIEW (2026-08-15) found 10, incl. a CRITICAL of my own making: restoreDefaultThemes() never cleared noDefaults, so on a start-fresh account the always-visible 'Restore default 5' button DELETED the user's themes and restored nothing, while toasting success. themes/noDefaults now move together through useDefaultThemes()/useNoThemes(). Also fixed: a tour could outlive its session and be inherited by the next account (its Done then wrote 'seen' against the WRONG uid); the bubble covered modals (a modal now stands the tour down via MutationObserver); Back could not step past a skipped step; a zero-height anchor could be ringed; the calc tour was a silent dead button on an empty board; and impNo started the tour twice."]}, {"id": "E7.3", "epic": "E7", "stage": "done", "platform": "web", "depends": "E7.2", "spec": "docs/features/E7_first-run-onboarding.md", "title": "The guide is shown once, and the button says Help", "notes": ["USER-FOUND (2026-08-27): the guide on every tab reappeared at every sign-in. Owner: users should see it once, then reach it from a Help button when they want it.", "Root cause: the flag recorded FINISHING a tour, not being SHOWN one. markTourSeen ran only from coachEnd(true), so every other exit wrote nothing - switching tab, a modal opening, the gate going up at sign-out, or stepping back off step one. Anyone who did not click Done through all seven steps of every tab was shown all five guides forever.", "Fix: credit at the moment the bubble goes up, in coachStart. coachEnd no longer takes a 'seen' argument at all - the question it answered no longer exists, so the parameter and all nine call sites lost it.", "Also fixed, same cause: tourKey fell back to a shared 'anon' key when there was no uid, so a guide dismissed before a uid existed was recorded against nobody and shown again to the real account. No uid now means no memory, and no memory means do not show it - fail closed, never nag.", "The ? button now reads Help (header on web; the Guided tour row of the Account sheet on native).", "E7.2 recorded 'Verified: seen tours do not reappear'. That verification only ever walked the Done path, which was the one path that worked. The seven tests added here each leave by a path that used to write nothing, and three of them fail against the old code."]}, {"id": "E7.4", "epic": "E7", "stage": "done", "platform": "web", "depends": "E7.3", "spec": "docs/features/E7_first-run-onboarding.md", "title": "The empty board an invited user actually lands on", "notes": ["Now that people can be invited, the empty board is the FIRST thing they meet - so it was worth looking at as a stranger would. It told them two different things at once.", "The context bar correctly said 'create a theme on Model' until a theme existed. The Overview hero said 'build a portfolio on the Rebalance tab' UNCONDITIONALLY - and Rebalance, with no themes, only answers 'build your themes first'. Two answers to one question, on one screen, pointing at different tabs, one of them a dead end.", "Root cause is the same shape as E7.3 and the mobile-web labels: one fact with two readers, and only one was taught. Fixed by extracting firstStepHint() so both callers ask the same function - not by editing the wrong string to match the right one.", "Also: an empty chart still reserved its full CSS height, so a brand-new account opened on roughly 900px of blank cards - value chart, return chart and donut all tall and empty. They now collapse when there is nothing to draw and the card keeps its heading and its one line of guidance. Desktop Overview 1100px -> 383px.", "Found while fixing that: renderOverview's empty-value-chart branch returned EARLY, before rendering the return chart at all - so on a new account the return chart kept whatever height it had with nothing in it, and its own empty branch never ran. It is now rendered on both paths.", "tests/firstrun.spec.js. The interesting assertion is not the wording but that the hero and the context bar AGREE, plus a guard that a POPULATED account still draws its charts - the risk of collapsing empty ones is collapsing a full one. Three of the five fail against the pre-fix code. Verified separately that the real fixture still draws a full value chart with the equal-weight line, and that touch scrubbing still has geometry to scrub.", "Same pass, History: the chart collapses when empty like the Overview ones (it reuses the same is-empty rule rather than a second one), and the Danger zone - 'Clears every holding and the whole checkpoint timeline' - is hidden until there IS something to clear. On a brand-new account it was both the most alarming thing on screen and a guaranteed no-op. History view 800px -> 204px empty.", "Nearly recorded a false pass here: isInit() reads state.portfolio.holdings, NOT state.holdings, so a fixture setting the latter looks populated and is not. The first check said the Danger zone stayed hidden even WITH a book, which would have been a real bug - it was my fixture. Re-tested across three real document shapes: empty (hidden), holdings but no checkpoints (shown), and with a checkpoint (shown, chart draws). The test file records the trap."]}, {"id": "E11.14", "epic": "E11", "stage": "done", "platform": "web", "depends": "E11.13", "spec": "docs/features/E11_online-deployment.md", "title": "Idle lock did not sign out; verification links landed in the cached account", "notes": ["OWNER-FOUND 2026-08-27, during the first real end-to-end registration. Signed in as test-a, the 5-minute idle lock fired, a NEW account was registered, and clicking THAT account's verification email landed on test-a's portfolio. Reproduced exactly before touching anything.", "DEFECT 1 - lockOut() never ended the SESSION. Every deliberate exit (the Sign out button, Delete account) called sbSignOut() BEFORE lockOut(); the idle timeout called lockOut() alone. So the one path whose entire purpose is security cleared the screen, said 'Signed out after 5 minutes of inactivity', and left the token sitting in localStorage - the next page load restored it and walked straight back in. Root cause is that lockOut was never responsible for the session, so it only took one caller forgetting. The session now ends INSIDE lockOut, where no caller can forget it.", "DEFECT 2 - nothing read the URL fragment. Supabase puts the new session in #access_token=...&refresh_token=...&type=signup. The app ignored it, so clicking your own verification link never signed you in; it just opened the app, which restored whatever session was cached in that browser. sbConsumeAuthLink now strips the fragment first (so tokens cannot be replayed from history), signs out whoever was here - the person clicking the link is by definition not the person who was signed in - then verifies the token against /auth/v1/user and adopts it. It fails CLOSED: an unverifiable link lands on the gate, never on a portfolio.", "Either defect alone is a bug; both were needed for what the owner saw. Tested separately for that reason.", "Also handled: a link pasted into a tab ALREADY on this page changes only the fragment, which is a same-document navigation - init does not re-run. A hashchange listener covers it, and reloads only when a session was actually adopted, because reloading on the failure path would discard the reason and leave a bare gate.", "The reason had to be deferred: showGate(true) wipes the message area on the way in, so an accurate 'Email link is invalid or has expired' was being cleared before anyone saw it. It is now held and shown once the gate is up.", "tests/authlink.spec.js - three of the four fail against the pre-fix code. Two measurement traps recorded on the way: a 1500ms wait read the page before the token check returned and looked like a failure, and page.goto to the same path with a different fragment is a SAME-DOCUMENT navigation, so init never ran and the hash looked unhandled."]}, {"id": "E1.10", "epic": "E1", "stage": "done", "platform": "web + ios", "depends": "E2.1", "spec": "docs/features/E1.1_data-driven-themes.md", "title": "Two-level allocation: rank tickers inside a theme, not just themes", "notes": ["OWNER REQUEST 2026-08-27: the model was metric-driven up to the basket door and then went flat. Capital was split EQUALLY between the names inside a theme, so two names rated PEG 0.6 and PEG 3.5 received identical money - discarding the very judgement the model applies BETWEEN themes.", "The reuse is exact, not approximate: every scorer is a PURE function of one value (1/x for PEG, EV, P/E; log(x/1e9) for cap; max(0,x) for momentum), with nothing calibrated to theme-level aggregates. Verified before designing. So the same score -> normalise-per-metric -> weighted blend pipeline runs unchanged on a ticker's own values. valueKind avgMcap collapses to that company's market cap and momentum to its own momentum.", "OWNER'S BOUNDS, applied at BOTH levels: ceiling 1/(n-1), floor 1/(2n). 3 themes -> max 50%, min 16.67%. 5 tickers -> max 25%, min 10%. Always satisfiable for n>=2 because the floors sum to exactly 1/2 at every n and the ceilings to n/(n-1) > 1, so half the book is always free to distribute by score. n=1 is special-cased - 1/(n-1) divides by zero - and simply takes 100%.", "TRADE-OFF PUT TO THE OWNER AND ACCEPTED: the floor is always 0.5x equal weight, but the ceiling multiple falls from 2x at n=2 to 1.05x at n=20, so the model can always halve a weak name while its room to reward a strong one shrinks as the basket grows. A symmetric alternative (ceiling 3/(2n) = 1.5x equal, keeping his floor and reproducing his own 3-portfolio 50% example exactly) was proposed and DECLINED - anti-concentration was preferred to symmetry. Recorded so nobody 'fixes' it later.", "One implementation, used twice: boundedWeights() at theme level and again inside each theme. The within-theme code lives INSIDE computeAllocation because M, wN, carry and getFor are already built there - rebuilding them elsewhere is how the crumb validator and the concurrency cap each got fixed in one place and left wrong in another.", "applyThemeCap and perTheme are gone. perTheme's comment still said 'equal-weight within theme', which had become a description of behaviour that no longer exists.", "The Rebalance preview said '$X ea' per name, which was honest only while names were equal-weighted. It now shows the actual spread, e.g. $6,033 - $12,226.", "tests/weights.spec.js (6) property-tests the arithmetic core across n=2..20 and six score shapes: sums to 1, both bounds respected, and ORDER never inverted. tests/within.spec.js (6) covers the feature; 4 of its 6 fail against the old equal-weight model, and the 2 that pass are the cash-conservation guards, which had to keep holding. 53 tests pass.", "Two of my own mistakes worth keeping: I asserted min === floor for [100,1,1] and it failed against CORRECT code - the floor is a minimum, not a target, so once the leader caps the rest split what is left and land above it. And deleting applyThemeCap by its preceding comment silently took boundedWeights with it, because I had inserted the new functions below that comment; the engine tests caught it in one run."]}, {"id": "E1.11", "epic": "E1", "stage": "done", "platform": "web + ios", "depends": "E1.2", "spec": "docs/features/E1.2_add-remove-ticker.md", "title": "Editing a portfolio no longer throws you onto Rebalance", "notes": ["USER-FOUND 2026-08-27: adding a ticker to an existing portfolio jumped straight to the Rebalance tab, mid-edit, before the user had finished adding names. Owner: 'super annoying because I might not have done adding tickers'.", "Root cause: the refresh was implemented AS a navigation. setMembership ended with switchView('calc') whose stated purpose in the comment was 'refresh Max drift + rail footer' - the refresh was the goal and the navigation was an unwanted side effect of how it was done. It then also scrollIntoView'd the plan and toasted 'review the plan, then Apply': three separate yanks out of an edit the user had not finished.", "Fix: renderAll() repaints every view including hidden ones, so Rebalance is kept fully current without going there. The user decides when to leave. Verified the plan already contains a newly added name while the view has not moved.", "Only this one call site was wrong. The other automatic switchViews are deliberate and were left: after a full reset (you must rebuild) and after Start fresh (documented as landing the user where work begins).", "tests/navigation.spec.js - 3 of its 4 fail against the pre-fix code; the fourth (creating a portfolio does not navigate) passed before too and is a forward guard."]}, {"id": "E10.1", "epic": "E10", "stage": "done", "platform": "web", "depends": "E6.5", "spec": "docs/features/E10_history-retention.md", "title": "Keep only the last 10 checkpoints", "notes": ["OWNER (2026-08-15): \"We will keep only the last 10 edits the users made. Any earlier edits will be removed/dropped from the database. We can safely assume that no one will want to revert to backups 10 edits ago.\"", "WHY IT MATTERS: every checkpoint deep-copies the whole state (holdings, themes, membership, watchlist, overrides, metric config) and nothing prunes them. Enough rebalances and the PATCH body exceeds the request limit - saving then fails PERMANENTLY with a generic error and no diagnosis.", "TRAPS (all in the spec): head is an INDEX into versions[] and must be re-based in the same operation or undo/redo jumps to the wrong checkpoint; never prune below head or mid-fork; prune on WRITE not on read, or the document keeps growing in the database; prune inside pushVersion so no caller can bypass it; valueHistory walks versions[] so the Overview chart shortens - its note must not claim more checkpoints than it has."]}, {"id": "E10.2", "epic": "E10", "stage": "done", "platform": "web", "depends": "E10.1", "spec": "docs/features/E10_history-retention.md", "title": "Undo must not silently discard theme/membership/watchlist edits", "notes": ["OWNER (2026-08-15): \"I still would like the idea to be able to revert back to a historical version/moment of the portfolio. This does not limit to the last edits made, but even earlier changes are fine too.\" - so revert-to-any-point STAYS; the defect is only that edits made SINCE the last checkpoint vanish.", "PRE-EXISTING, NOT introduced by E5-E9 - restoreVersion is byte-identical to prod. snapshotState versions themes/themeTickers/watchlist, but createTheme/deleteTheme/renameTheme/setMembership/addToWatchlist never push a checkpoint. So Undo replaces them from an older snapshot and persists it, while the toast says 'Redo available' - and redo restores the same pre-theme snapshot. Unrecoverable.", "DECIDE FIRST: (A) checkpoint structural edits so nothing is ever lost - recommended, with only TRADE checkpoints counting toward E10.1's cap so a few theme edits cannot push a real rebalance out of the window; or (B) keep the semantics and warn plainly in the undo/revert confirm. B leaves the data-loss path open and only labels it."]}]};
